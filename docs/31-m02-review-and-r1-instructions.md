# M-02统筹复核与MR-05补修指令

2026-09-08，Codex。原M-02交付及证据保留原样；本文件记录之后的审核决定。

## 1. 结论

**M-02整体验收暂不通过，主台账改为阻塞（MR-05），等待用户转交ASTRA-M02-R1。M-03及后续保持未开始。**

资格、组合、抽取和MR-02稀缺候选生成层验证认可，保留现有实现；MR-02生成层缺口关闭。MR-05归M-02补修，不推迟到M-03，不新增产品机制。执行者遵守此前禁止修改持久化的边界并如实停止，本次需要统筹明确扩大这一处修复范围。

| 发现 | 判断与处置 |
| --- | --- |
| MR-05 / P1：合法稀缺到期市场在保存时丢失 | src/persistence/save.ts:1093附近validExpiryMarket要求恰好3外部，与已批准0–3外部冲突。作为M-02阻塞，补修后才能验收。 |
| MR-02：受控稀缺候选生成 | 48组选择层、9组公共生成连接及角色门槛验证通过，接受生成层覆盖；不等于保存、读档、签约全链路通过。 |
| MR-03：重入与偏好 | 仍归M-03；本次返工不改变窗口推进、回看导航或偏好行为。 |

当前静态目录及常规100种子未触发稀缺存档问题，不表述成所有玩家都会丢失报价。但“候选不足仍可继续”是本轮明确验收要求，不能因自然样本少而豁免。

## 2. 独立核验与证据

- 审查M-02前后transfers.ts差异，唯一生产改动在该文件。成人平台资格、两地区保证、海外高平台两槽加开放槽、救济严格改善、续约包装与批准规则一致；不调整本批分布参数。
- 独立定向6文件78项通过、typecheck通过。包括8份原始存档直接恢复及后继、48组稀缺选择、9组生成连接、47组原边界和3类持久化反例。
- 三方向各100窗口：CONDITIONAL国内103/300，每窗至少国内海外各1；DOMESTIC国内203/300，每窗至少2国内；STRONG国内3/300。后者为本输入分布，不是全局概率或必出承诺。
- 额外4项独立公开动作复现：从冻结到期档取完整续约及外部0/1/2/3的明确构造子集，在全新Storage中调用selectTransferChoice触发真实commit/saveGame。0/1/2外部时，内存仍为TRANSFER_WINDOW，但原始写盘envelope已为PRO_STAGE_COMPLETE、窗口回最后history且报价清空；loadGame和continueCareer一致读出清空结果。3外部对照完整保留。现金、history、事件和报告不重复结算。这4项通过是“反例复现正确”，不是功能通过。
- 独立核验39项M-02交付产物SHA/字节、564项此前文件，其中555项未变，9项修改符合声明；11项改动/新增文件after哈希一致，M-01原72项产物仍一致。原训练、store、save、页面、目录和outputs未被M-02改动。
- 本轮不重复全量/build，采用哈希对应的执行者原日志（63文件460项、typecheck/build通过）。本轮没有浏览器/手机/线上验证，保留大包警告。HEAD仍为3f290407a299790665b58c4bc7c2bf8455d24bbd，版本12/12。

独立复现源码、原始日志和写盘前后状态见[复核证据](evidence/CEU-20260907/M-02-review/README.md)。统筹未修改生产代码，未提交推送部署。

## 3. 最小修复契约

修复限于到期TRANSFER_WINDOW的持久化规则：接纳1份现队续约＋0–3份外部自由转会，唯一性按实际数量判断。保留原始合法报价和谈判字段，不调用生成器补满、不重新抽取、不改phase/window。

不能只移除整个validExpiryMarket保护。对到期市场确认完整结构：恰好1份现队续约；其余均为FREE_TRANSFER且不含现队；外部俱乐部及报价ID不重复；总量符合上限；选中项对应合法报价，不能用STAY绕过到期签约。保留现有基础schema、年龄退役及损坏数据处理机制。谈判撤回的报价记录本身仍应保留，不能把包含withdrawn报价的正常市场整体清空。

当前无新增持久字段或枚举，本修复按12/12执行，不另加迁移、不声称旧客户端兼容已验证。若实际发现必须更改格式或公共store行为，先交具体反例和最小建议，不扩大到M-03。

## 4. ASTRA-M02-R1完整转交指令

配置沿用GPT-6 Astra中度。以下内容由用户转交后开始；当前仅准备指令。

```text
你执行《上场》CEU-20260907的ASTRA-M02-R1，只补修MR-05，由Codex统筹验收。
项目：/Users/zhihu/Documents/足球生涯游戏。
本次用户转交授权将M-02范围扩大至到期市场的必要持久化校验与对应测试；不启动M-03或后续，不提交、推送、部署，不创建代理。

先读适用AGENTS.md、docs/20第5/10节、docs/23第2节、docs/30、docs/31全文、docs/21市场栏及M-02/README、handoff-and-blocker、M-02-review/README。
预期HEAD为3f290407a299790665b58c4bc7c2bf8455d24bbd，SAVE_VERSION/DATA_VERSION=12/12。核对git status与本批前哈希，保护全部未提交训练和市场改动、冻结S0/T/M-01/M-02证据及outputs，不访问用户真实存档。
收到本次授权后，将M-02从阻塞改为进行中，统一主台账顶部、任务行及第10节；MR-05保持未关闭。MR-02生成层已验收，不重新调分布或扩大种子搜索。

一、修复
生产修改限定src/persistence/save.ts的到期TRANSFER_WINDOW校验及其直接必要辅助逻辑。将“恰好3外部”改为“0–3外部，唯一数等于实际数”，同时按文档31第3节检查一份现队续约、正确报价类型、外部排除现队、去重和有效选择。
保留基础schema、年龄/退役及损坏档保护。合法稀缺市场必须完整保留phase/window、报价顺序、ID、合同数值、谈判成功/失败/撤回字段和当前选择，不补满、不重生成。
不得通过只放宽max长度、删除整个异常修复分支、把稀缺档退回结束页重开、修改生成器强凑3份等方式规避。
不修改transfers.ts、gameStore生产动作、页面/导航、训练、球队数据、依赖、版本或迁移。不做求职方向编辑和MR-03防重抽。
若可靠修复确需上述范围外代码或新格式，停止依赖工作并提交具体证据，不自行扩展。

二、验证
1. 将M-02中3组稀缺持久化反例改为成功保存/恢复的目标断言；历史pool-connections.json和复核反例不改。新证据仅写M-02-R1目录，独占写入，默认测试只读。
2. 覆盖可签约年龄下续约1＋外部0/1/2/3。优先复用已有受控候选目录，真实generateContractExpiryOffers生成后，经公开store入口及saveGame/loadGame验证。构造来源、差异与真实调用边界必须清楚；不能只对validateGameState断言后声称端到端通过。
3. 对每组确认内存状态、首次实际写出的envelope、清空store后从原始envelope加载、continueCareer、重复保存重载完全一致；各例新Storage且无backup，避免备用档掩盖数据丢失。原始envelope首次读取前不重存data。
4. 对0外部验证续约可确认并进入下一计划；有外部时分别从独立起点验证续约和一份外部签约，经合法到队NONE/完成转会进入计划。保存/加载不退回结束页、不额外推进窗口；后继不重复cashEuro/history/事件/报告结算。
5. 从真实公开谈判得到代表性的成功/失败/撤回结果，检查稀缺市场中字段和选择在保存/加载后原样保留。复用固定有限输入；若缺某自然结果，另标构造状态只验证持久化，禁止无限搜索或冒称公开产生。
6. 有限异常对照：无续约、重复续约、外部重复、续约队错误/外部含现队、混入其他报价类型、选中项不存在或到期STAY，确认不能作为合法到期市场放行。沿用项目对损坏档的拒绝/修复约定，明确实际结果，不扩大为全存档重构。
7. 原M-01的8份原始envelope恢复继续通过，覆盖已有谈判；年龄禁签、末期到期退役、有效合同下空市场合法STAY及现有11→12训练迁移断言保持通过。普通空市场不等于到期无续约档，不混用两者规则。
8. 执行相关定向测试（新增R1、persistence、M-02市场/store）、全量test:run、typecheck、build，保存实际日志；冻结哈希核对。无UI修改，不要求新增浏览器/手机验收，写明未执行，保留大包警告。不得把MR-03仍存在当成本批放宽测试的理由。

三、交付
新建docs/evidence/CEU-20260907/M-02-R1/，包含README、修复前后对应表、4种数量及签约后继证据、谈判/异常/旧档覆盖、原始日志、修改清单、前后哈希和冻结保护结果。
同步docs/20、21及确受本修复影响的规则说明。M-02完成补修后统一标为待验收，MR-05标“补修待统筹验证”，不能自行关闭；M-03及后续未开始。
最终报告实际测试数量、保存/恢复/签约结果、异常与旧档结果、HEAD、限制和未执行项。
完成后停止，唯一下一动作：用户转交Codex复核ASTRA-M02-R1。
```
