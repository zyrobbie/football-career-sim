# M-04统筹验收与F-01交接

2026-09-08，Codex。原交付及冻结证据保持；本文件记录后续验收与下一批边界。

## 1. 结论

**M-04通过，主台账标为已完成；M-01至M-04按批准范围收口，MR-03/MR-05保持关闭。** 未发现阻塞市场专项验收的新问题。F/Q/R尚未开始；市场通过不等于整次升级或发布验收通过。

市场资格、三方向组合、外部最多3份与到期续约另计、稀缺保存恢复、下一市场偏好生效、已有报价和谈判稳定、签约后继及有限连续流程均有对应证据。已接受的自然救济样本缺口、构造稀缺/撤回标注继续保留，不追加无界搜索或调整概率。

## 2. 独立核验与边界

| 层次 | 本轮结果 |
| --- | --- |
| 源码与交付 | 审阅新增10项测试及观察方案，核对56项交付产物；877项既有文件中874项未变，声明变化仅docs/20/21/23，584份冻结证据及outputs保持；生产源码未改 |
| 工程 | 独立定向16文件207项、typecheck通过；全量66文件520项及build采用已核验的M-04原日志，本轮未重复执行；857.65kB大包警告保留 |
| 原档与状态 | 独立定向覆盖M-01原8份及R1原7份raw、14组构造旧phase恢复、签约后继；原档读取不先重存或借backup修正 |
| 连续窗口 | 核对3固定种子×3策略×6窗，共54个独特窗口、15次实际市场生成，9条分支全部回到HALF_YEAR_PLAN；定向复跑不另计为新增54个独特样本 |
| 页面 | 独立临时Chromium在本地4189复跑5主组＋2补充签约CTA组，1280/390/430共25图与交付逐字节一致；320沿用已核验旧证据，本轮未重跑 |
| 实际目视 | 本轮查看390稀缺签约CTA、430滚动保存、390报告回看、1280市场首屏四图；不声称25图全部独立目视。主5组console error/warning为空，补充2组脚本未收集console |

连续记录中的 `inputEnvelope` 实际保存的是**最终存档**，与 `final` 一致；交付README已解释。作为非阻塞命名限制接受，不改冻结JSON。复演起点使用记录的 `input`，结合原来源及预定seed构造说明，不能误用 `inputEnvelope` 当起点，也不能把构造seed路径称为自然采集历史。

真实页面使用匿名数据和隔离context，未读取用户浏览器档；复核脚本只改输出目录及原结果来源定位，保留原断言。浏览器及本轮本地服务均已关闭。独立定向/typecheck的原始输出位于本会话工具结果，未另存完整日志；持久工程日志引用原M-04，明确区分。

独立证据见[M-04-review](evidence/CEU-20260907/M-04-review/README.md)，原交付见[M-04](evidence/CEU-20260907/M-04/README.md)。430固定底栏在部分滚动位置的遮挡、既有小字/省略和桌面留白保留；正常滚动后按钮可达已验证。真实手机、其他浏览器、线上、旧客户端读取v12及发布回退未执行，有限观察不代表最终平衡。

HEAD仍为 `3f290407a299790665b58c4bc7c2bf8455d24bbd`，版本12/12。本轮只更新统筹文档和新增复核证据，未修改生产、未提交推送部署、未创建代理。

## 3. 下一批安排

建议F-01由Astra轻度执行，仅梳理当前流程、锁定变更前基线和F-02验收契约。已批准D-F1继续有效，不重新讨论产品方向。M-03已解决只读回看、旧市场恢复与部分phase守卫，不能照搬S0旧缺陷为当前事实。K-09失约留队入口仍待F核实处理。下面指令待用户转交，当前没有启动执行。

## 4. ASTRA-F01完整指令

```text
你执行《上场》CEU-20260907的F-01，使用GPT-6 Astra轻度，由Codex统筹验收。
项目：/Users/zhihu/Documents/足球生涯游戏。
用户转交本指令后，只授权F-01：盘点报告后的全部入口、锁定当前流程与已批准目标的对照、建立有限基线及F-02交接。M-01至M-04已验收，MR-03/MR-05关闭；F-02及后续未授权。不修改生产行为，不提交、推送、部署，不创建代理。

一、前置与保护
读取适用AGENTS.md，docs/20第4、5、6.3、7、10节，docs/21当前结论，docs/23第3节、docs/25的D-F1、docs/32–34及相关证据索引。旧文档中的历史缺陷和“未开始”以当前源码和主台账为准。
核对HEAD预期3f290407a299790665b58c4bc7c2bf8455d24bbd、存档/数据12/12、git status。记录本批前哈希，保护全部已有未提交生产、测试、冻结证据及outputs；旧采集开关保持拒写，不覆盖旧夹具，不读取用户真实浏览器profile或生涯。
将主台账顶部、F-01行和第10节一致标进行中。允许新增F-01专用测试、匿名夹具、证据与流程文档，更新台账；不得为使基线通过修改src生产、市场/训练参数、存档字段/迁移、依赖或页面样式。

二、锁定当前→目标流程表
先追踪HalfYearReportScreen、ProfessionalStageCompleteScreen、TransferWindowScreen、RetirementScreen、App、gameStore、marketContext、careerTime及国家队退出相关实现，不依据按钮文字猜行为。
逐行记录：输入phase、当前windowIndex W、最后history窗口H、合同/年龄/机会等条件；可见主次按钮；store实际允许条件；实际动作和目标phase；W/history/cash/报告/事件/国家队记录应变化或不变的字段；保存恢复语义；当前点击数；批准目标点击数；源码位置及证据。
至少覆盖：
1. 青训未满4窗、第4窗首份合同链路；保持原范围，不混入职业页合并。
2. 普通职业报告、首个职业半年、有正式机会、已有市场及已经处理完的市场。
3. 合同到期、0–3外部另加续约；不能跳过合同处理或将到期留队误作STAY。
4. 连续失约且无正式机会、连续失约同时有正式机会。复现K-09的UI/store不一致，明确当前实际结果。批准目标：失约“继续留队”在有正式机会时进入市场并选中STAY，仍须确认；无正式机会时直接下一计划。F-01只立证，不修代码。
5. 自愿退役可用/不可用、强制退役、末期到期、最后一年合同仍有效；由careerTime实际窗口/年龄判定核实边界。
6. 可退出国家队与不满足条件；不可撤回确认、取消不生效、退出后俱乐部仍可继续。
7. 自愿退役取消回原报告或旧结束上下文的目标，对照当前取消行为；强制退役不可取消。
8. 旧PRO_STAGE_COMPLETE、旧报告、已有市场/签约后继恢复，只读回看及返回，事件结果与SIMULATION_READY恢复，避免越过待处理事件。

批准优先级：已处理上下文只读/恢复→强制或末期到期退役→合同到期→连续失约→正式机会→普通下一计划；自愿退役及退出国家队为合法次要入口。普通职业报告一次操作到计划，市场直接到相应决定；保留报价选择、续约/留队确认、退役和退出国家队必要确认。
将“当前已由M-03完成”与“F-02仍需实现”分列。只读回看不改phase、原报价复用和旧市场W=H+1恢复不得重做成旧方案。不要统一假设所有状态W=H，或每次动作都W+1。

三、有限基线与证据
先写case-plan，覆盖上述分支，每个关键分支一个代表输入，共用边界，不做条件全排列或无界种子搜索。优先复用S0、T、M的匿名来源，直接读冻结raw；必要新采集用当前12版公开动作保存的新目录，记录来源、版本、seed、选择序列及哈希。
无法自然到达的特定边界可用明确标注的构造输入，列出来源与修改字段，经实际save/load及合法后继验证；不冒称真实采集。冻结v11仅用于原档迁移读取，不能用当前版本重造v11。M-04/results.json的inputEnvelope是最终存档，起点应使用input及来源说明。
定向测试验证实际现状、状态转移及现有不重复结算契约。K-09作为明确缺陷复现单列，不能把错误行为混作未来正确性标准。未来F-02断言单独列出，不用大量skip/todo伪装当前测试通过。
旧报告/结束页加载不重算；已有市场先比较完整报价、谈判、选择，再做合法后继；确认history前缀、cash、事件/国家队记录只有动作允许的变化。SIMULATION_READY首次继续按现有契约可恰好模拟一次，与已结算报告分开。
基线步骤按“看到报告到下一项实质决定”的主动操作数记录。隔离本地Chromium用1280×720和390×844各验证一条普通报告流程与一条代表特殊流程，实际点击、保存/刷新、截图查看；K-09若可用匿名构造页稳定呈现，补一组真实点击。保留必要确认，不把自动动作计作玩家点击。真实设备/其他浏览器未执行则直写。
复用已核验市场/训练证据，不重跑54窗或全量年龄矩阵。运行新增及受影响的报告/store/导航/恢复定向测试和typecheck，保存完整命令、退出码及原始日志。无生产改动时无需为了数量重复全量/build；发现相关失败再有限排查。

四、F-02交接设计
产出一份当前→目标流程对照文档及证据README，链接到docs/20、21。列清最小改动文件、共享动作判定与一次状态提交的职责、旧结束页兼容、退役取消来源及必要守卫，不在F-01实现。
沿用批准的共享professionalNextAction和受控推进方向，避免报告先commit结束页再调用旧动作的双写拼接；复用现有marketContext恢复。区分已结算报告W=H、已生成市场W=H+1与已签约后继。列出陈旧回调/双击/重载的F-02断言及F-03真实点击验证，禁止重模拟、重发薪、重复history/事件/国家队记录。
如果确需新增持久字段、升级存档或扩大规则，说明具体无法复用的原因和最小替代，交统筹决定；本批不得自行实施。

五、收口与停止
证据写docs/evidence/CEU-20260907/F-01/，附来源清单、case-plan、结果、复跑命令、截图记录、变更文件及前后保护核验。统一主台账顶部/F-01行/第10节为待验收，记录K-09现状及F-02待办，既有M专项已验收不改回待验收。
最后只报告交付路径、当前与目标流程差异、已执行验证、构造/未执行/阻塞、HEAD和版本、未提交推送部署。
完成即停；唯一下一动作：用户转交Codex审核ASTRA-F01。不得进入F-02或其他后续阶段。
```
