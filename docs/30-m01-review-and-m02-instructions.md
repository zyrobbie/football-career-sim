# M-01统筹验收与M-02实施交接

2026-09-07，Codex统筹复核。执行者交付索引与冻结文件保留原状，本文件记录后续验收决定。

## 1. 验收决定

**M-01通过，状态改为已完成。M-02未开始，待用户明确转交实施指令。** 本次仅接受市场变更前基线，不代表新市场规则、偏好编辑或防重抽已经完成。T-01至T-04结论保持。

| 决定 | 处理与后续责任 |
| --- | --- |
| MR-01 自然救济存档缺口 | 接受为M-01非阻塞限制。59份已有来源未找到自然起点；构造差异公开，经过真实保存、加载及公开市场动作，结合源码足以支撑下一步。M-02继续验证救济触发、角色严格改善与回退；不再为此扩大完整生涯搜索，不称自然路径已覆盖。 |
| MR-02 真实候选不足存档缺口 | 接受基线缺口，但将动态稀缺池测试列为M-02必交项。构造空报价只证明保存/重入语义，不能证明生成器无候选或回退正确。用实际生产选择逻辑的受控候选池验证0/1/2名、地区不足及救济角色不足。 |
| MR-03 重入缺陷分期 | 到期市场重抽、空市场重抽、普通市场恢复被节奏拒绝均保留给M-03。M-02只改新生成的资格/组合/抽取，不能因引擎通过就把不重抽风险关闭或提前发布。 |
| MR-04 救济条件释义 | 文档23“保留现有触发”按已核验源码明确为：海外tier≤2、一线队，且FRINGE，或SUBSTITUTE且出场≤5。FRINGE不额外要求≤5。这是消除文字歧义，不新增产品规则。 |

撤回补采实际同时构造seed和brokenPromiseWindows=2，超出最早“只改seed”的书面策略；差异及18次上限均已完整披露，接受为构造补证，不归入自然存档。初版类型、构造校验和错误预期失败已留档，不要求重新采集。

## 2. 本次独立验证

- 定向4文件38项通过：M-01、S0市场基线、transfers、v1ClubWorkflows；typecheck通过。独立复跑包含E-03完整100份报价对照、47组边界、存档后继和有界撤回搜索。
- 另补8项原始envelope直读：每份直接放入全新内存Storage，初始无backup，先loadGame再continueCareer，完整状态相等，重存重载仍相等。没有先取data再saveGame来替代原始envelope读取。源码及日志见[复核证据](evidence/CEU-20260907/M-01-review/README.md)。
- 独立核验72项交付产物SHA/字节、485项采集前文件，其中483项不变，仅docs/20、21符合声明的修改；所有已有src、冻结证据、outputs未变。8份最终存档及来源哈希、12/12版本、cashEuro/报价/选择核对通过。
- 源码确认资格硬排除、组合槽、角色改善及市场重入分支，与交接事实一致。有效输入仅M-01/generated-r4和supplement；早期generated/r2/r3不纳入验收。
- HEAD仍为3f290407a299790665b58c4bc7c2bf8455d24bbd。本轮未改生产代码、未执行全量/build/浏览器/真实手机；既有构建警告和发布限制保留。本轮新增直读检查属存档验证，不是页面或完整生涯验收。

## 3. 下一批完整指令：ASTRA-M02

建议配置：GPT-6 Astra，中度。以下指令由用户转交后才开始；本次统筹未发送、未启动实施。

```text
你执行《上场》CEU-20260907市场专项M-02，使用GPT-6 Astra中度，由Codex统筹验收。
项目目录：/Users/zhihu/Documents/足球生涯游戏。
本次用户转交授权仅M-02：实现新市场候选资格、报价组合与确定性抽取，完成对应验证。M-01已由文档30验收；不进入M-03/M-04/F/Q/R，不提交、推送、部署，不创建代理。

一、先恢复上下文并保护现场
读取适用AGENTS.md，docs/20第4/5/6.2/7/10节、docs/23第2节、docs/25产品确认、docs/30全文、docs/21市场栏，以及M-01/README和facts-and-handoff。
核对git status、HEAD与12/12版本；预期HEAD为3f290407a299790665b58c4bc7c2bf8455d24bbd。以实际工作树而非HEAD代表训练改动，保存本批前哈希清单。T-01至04已验收，保护全部已有改动、S0/T/M-01冻结证据和outputs。不得访问用户真实浏览器存档。
确认授权后把docs/20顶部、M-02行、第10节统一记录为进行中。M-01保持已完成。

二、改动范围
主要修改src/engine/transfers.ts及必要的小型共享纯函数，配套engine/store测试、规则文档与证据。可导出真正由生产生成器调用的资格/槽选择纯函数供分层测试；不复制一套测试专用算法，不为稀缺测试改球队目录或加入运行时测试开关。
不修改gameStore生产动作、页面、导航、偏好编辑、存档结构/版本、训练规则、球队数据、依赖。generateContractExpiryOffers继续复用外部生成结果，必要适配限于保持续约契约。若本批无法在此范围可靠实现，写明具体阻塞交统筹，不靠越界修复掩盖。

三、按资格→组合→抽取实现已批准规则
1. 常规成人资格：OVR≥85允许一级联赛且platformTier≤3，或中国一级且platformTier=4；80–84允许一级且platformTier≤4，不限国家或intent。74–79、66–73、<66原档保持。使用现有兼容球队参数，区分联赛级别、平台与club.tier。兴趣分或抽样结果不能代替资格判断。
2. 成人外部最多3份：DOMESTIC先2国内槽再1开放槽；CONDITIONAL两地均有合格候选时各1再1开放槽；STRONG先首偏好海外联赛、其他海外，再开放。国内有至少2队时DOMESTIC每窗至少2国内，两地均有队时CONDITIONAL每窗各至少1。地区限定只用于相应槽，开放槽包含全部剩余合格队。
3. OVR≥85且STRONG，在候选足够时前两槽保留高平台海外目标（沿用现有club.tier≤2语义），并保留真正的第三开放槽。先在这两个槽内协调高平台与联赛偏好，不能额外插入第三个保底后slice掉开放槽。其他intent按自身地区槽规则执行，不强加两份海外目标。
4. 普通槽不足时依次使用该槽候选、同地区其他合格队、全部剩余合格队；需要保证的地区槽先得到满足，避免fallback提前消耗另一槽唯一候选。无剩余只少给，不造队、不重复、不含现队外部邀请。保持候选目录稳定顺序和ID。
5. 抽取沿用interest×0.65+fit×0.35、首偏好×1.28及原rotation语义；各槽固定、清晰且互不混淆的namespace。相同完整输入多次输出严格相同；不使用时间或非确定性随机，不调薪资/角色/潜力估计公式来达到地区目标。
6. 到期保留独立现队续约最多1份，外部复用新政策0–3份并转FREE_TRANSFER，转会费用0，总数≤4。不得把总报价slice为3。保留续约原随机流及合同规则。
7. 救济保留现有触发：当前海外club.tier≤2、一线队、FRINGE或（SUBSTITUTE且出场≤5）；FRINGE没有出场≤5限制。所有候选及fallback必须承诺FIRST_TEAM且角色序位严格改善，不套成人高OVR平台筛选。
   槽为五大联赛较低平台、发展联赛、国内ELITE；DOMESTIC先国内槽，其余方向沿原顺序。不足在剩余角色改善池回退，再不足少给，不放宽角色门槛。
8. 未满18仍仅国内实际转会，16–17海外关注保持；40岁不能新签约。市场开启节奏、表现门槛、失约申请、年龄与合同限制保持。不新增搜索球队、刷新按钮或市场频率。

四、先划清旧基线与新断言，再验证
1. S0/M-01旧JSON和源码副本只读保留。M-01采集开关在当前测试代码中显式拒绝写入；原副本仅留档。生产规则变化后，不能继续用原输出相等测试要求新市场保持旧缺陷，也不能覆盖原JSON为绿灯。
   将相关S0/M-01市场生成断言改为批准目标，或拆到明确的新专项测试；列出逐项替换对应表，保留旧档恢复、当前有效行为以及M-03尚未修复风险的覆盖。不得整文件skip或删除失败检查，不能通过捕获错误当成功。
2. 直接复用E-03原完整输入及100种子，对三方向各做成对对照。CONDITIONAL每窗至少国内/海外各1；DOMESTIC合格国内足够时每窗至少2；STRONG证明中国在资格池且开放槽在受控随机输入下能选中国。STRONG的100种子只记录分布，不虚设国内必出频率。
3. 复用M-01的47组完整输入覆盖年龄17/18/33/37/40、OVR79/80/84/85、境内外、三方向和三路径；不要求全排列。增加必要的直接资格断言，证明不因intent硬排除，而不只凭抽样推论。
4. MR-02强制补测：通过实际生产纯函数或隔离依赖控制候选输入，验证总池0/1/2名、无国内、仅1国内、无海外、首偏好无队、高平台不足、某地区唯一队与重复/当前队排除。每个结果有明确期望与来源标注，连接真实公共生成器及到期包装验证。不能把40岁年龄禁签返回[]当成0候选算法测试。
5. 救济覆盖FRINGE（包括出场>5）、SUBSTITUTE出场5/6、年龄限制、DOMESTIC国内优先，以及无严格改善/仅1或2个改善候选、各槽缺失；所有返回和fallback均严格改善。自然救济档缺口已获M-01范围接受，本批无需另开完整生涯种子搜索。
6. M-01最终8份原始envelope逐份直接置入隔离Storage（无backup），先loadGame/continueCareer，再公开合法确认/到队/下一计划、保存重载。不得先saveGame(data)替代原envelope加载；完整报价、选择、成功/失败/撤回谈判、draft及当前方向在加载时保持原样，后继不重复现金/事件/history/报告。使用generated-r4的7份及supplement/withdrawn.json，不能混入早期中断产物。
7. 明确当前重入缺陷仍归M-03。M-02不得改市场window、回看或复用逻辑以顺手消除它；旧全输出比较受新生成规则影响时改用行为断言，保留缺陷证据，不把尚未实现的防重抽写成通过。
8. 完成相关定向测试、全量test:run、typecheck、build并保存原始日志。训练已有断言继续通过。本批无UI改动，浏览器/真实手机不作为完成要求，写明未执行；原大包警告保留。

五、交付并停止
证据只新增docs/evidence/CEU-20260907/M-02/，附README、完整输入/输出与分布、稀缺池来源/期望、8档直读与后继、命令原始日志、前后哈希、逐文件改动及旧断言替换对应表。新输出独占写入，测试默认只读，严禁覆盖历史证据。
同步docs/01、02、05中确受市场规则影响的内容及docs/21市场栏；不把M-03偏好编辑或防重抽写成已交付。更新docs/20顶部、M-02行、第10节为待验收，记录限制及唯一下一动作“用户转Codex审核M-02”。M-03及后续保持未开始。
交付摘要包含实际测试数量、8档结果、稀缺池覆盖、市场分布及限制、HEAD、未执行项。完成后停止；不得自行宣布市场专项整体完成或进入下一批。
```

## 4. 当前检查点

S0、T与M-01完成；仅M-02指令准备完毕。用户明确转交后由执行者开始，完成后仍由Codex验收。MR-02必须随M-02关闭，MR-03必须在M-03及后续验收追踪，不能因本次基线通过消失。
