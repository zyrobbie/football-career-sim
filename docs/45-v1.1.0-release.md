# v1.1.0 生涯体验升级发布记录

2026-09-10。用户已明确授权推送 main 并由 GitHub Pages 自动上线。目标仓库为 zyrobbie/football-career-sim，线上地址为 https://footballcareer.zyrobbie.site/ 。

## 内容与验证

本版交付适龄训练、可修改的求职方向、报告直接分流，以及报价说明、退休图片布局修复。T/M/F/Q 全部已验收，用户已确认六项体验通过。SAVE/DATA 保持 12/12，产品版本更新为 1.1.0。

发布检查补齐独立的 `career_save_v11_backup`：首次升级或保存前保留校验通过的 v11 原始字节，之后不随滚动备份覆盖；备份写入失败时停止覆盖原档；用户明确删除生涯时一并删除。没有更改训练、市场或报告规则。

工作区全量 72 文件／722 项测试、typecheck、build 通过，其中新增 6 项独立备份测试；在基线提交的隔离副本另验证 2 项旧客户端读取行为。只包含拟提交文件的干净副本也通过72文件／722项测试、typecheck与build，复用本地已安装依赖；GitHub Actions将独立执行npm ci。主包约 859.75 kB，保留大包警告。

## 提交边界

- 包含已验收生产代码、正式测试、依赖版本元数据、产品与升级文档、测试必需的 50 份冻结输入及选定验收证据。
- T-02 开始时的已跟踪源码与原 HEAD 一致，已核对，不将早期文档修改混称新功能。
- 完整过程证据仍在本地；大型逐步状态日志、未选截图、outputs、dist、临时浏览器及本地试玩脚本不纳入提交。仓库中的历史证据索引保留原文，部分链接只在完整本地工作区可用，见 [证据范围](evidence/README.md)。
- 具体清单与哈希见 [发布证据选择](evidence/CEU-20260907/R-01/evidence-selection.json)。GitHub Pages 只上传 dist，测试存档不会随站点分发。

## 存档与回退

旧客户端无法读取 v12：已在 `3f290407` 的隔离客户端实测拒读且未修改原始 v12；同一客户端可以读取冻结 v11 原档。因此不能直接部署旧代码并声称存档已回退。

优先以保留 v12 读取能力的修复版本处理线上问题。确需恢复 v11 时，先停止该浏览器的游戏写入，导出当前 v12、滚动备份及 `career_save_v11_backup` 到独立文件并校验保存成功，再在隔离旧客户端中验证 v11 备份；经用户确认后才把原始 v11 恢复为当前档。恢复后进度回到升级前，不合并 v12 新进度。没有 v11 备份的新生涯必须保留 v12 客户端处理，不能强行降级。上述实际用户存档恢复不在本次发布中执行。

## 发布状态：已完成

产品提交：[321f747](https://github.com/zyrobbie/football-career-sim/commit/321f747c3a06e41ed4fc2868778e0aa3a3d9134a)已推送main；[GitHub Pages运行34456233963](https://github.com/zyrobbie/football-career-sim/actions/runs/34456233963)的独立依赖安装、测试、构建、部署全部成功。正式地址：[上场](https://footballcareer.zyrobbie.site/)。

| 线上核验 | 结果 |
| --- | --- |
| 页面身份/可达 | 正式域名200，标题“上场 · TAKE THE FIELD” |
| 资源版本 | index-BD1ydlda.js与index-DSx3aor_.css逐字节匹配干净构建 |
| 非空/错误覆盖层 | 首页与建档正常，无错误覆盖层 |
| 控制台 | 本次操作无警告/错误 |
| 操作 | 内置浏览器1280×720：开始新生涯→匿名姓名→继续至位置步骤2/5 |
| 目视 | 已在工具会话中检查第2步截图，未声称另存截图文件 |

核验使用线上初始空存档入口，未访问用户真实生涯；临时标签已关闭。详见[资源比对](evidence/CEU-20260907/R-01/online-assets.json)、[Actions结果](evidence/CEU-20260907/R-01/actions-result.json)、[收口结果](evidence/CEU-20260907/R-01/release-outcome.json)。后续收口提交仅更新文档和发布证据，不改变此产品提交的应用资源。

保留大包警告及Actions旧action运行时弃用提示；未在本次线上重新跑完整职业路径，不扩展到未测浏览器。用户已确认相册保存通过但未提供设备型号。R-01/R-02/R-03全部完成。
