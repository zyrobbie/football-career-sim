# PSU V1.9：统筹S2结论与S3续令

2026-09-12。当前用户已授权Codex指挥「开发员 Astra 轻」完整升级并验收，产品版本V1.9。

## S2统筹结论

统筹已审阅配置和职业模拟diff、原精确断言变化、Ajax独立公式推导、1500+62配对、5组伤病及维护10000扩样、48窗口及首次外部路径分歧，核验全量74文件741项、typecheck通过。独立只读复跑四项runner及维护10000扩展通过，日志在 `/tmp/PSU-20260912-coordinator/s2-replay.txt` 和 `s2-injury-extended-replay.txt`。

**S2实质验收通过，采用主规格默认参数，0轮调参。** 已知限制：48窗没有实际执行恢复窗，两个PUSH分支第4窗末身体41；不能宣称已验证其后恢复。连续外部路径存在自然分歧，属于有限观察，不证明全生涯绝对平衡。

执行者补齐S2最小README、源码补丁/新增源文件快照和最终保护记录。保护无异常后同步S2已验收并直接执行以下S3；若存在未解释范围漂移，先报统筹。S1/S2原输出不刷新。

## S3：WP-05 / WP-06 / WP-08

1. **数值冻结。** 不再改配置数值、比赛区间、风险、角色或维护规则。可为说明新增局部执行元数据和必要只读helper，不增存档schema。
2. **报告。** 由实际准备/比赛执行生成eventSummary，持久保存后回看不重算；恢复明确“策略未执行”；状态/关系收益上限、风险下限或机会范围封顶如实说明。不能声称额外获得固定场数或把本次伤病断言为策略导致。保留原维护、事件、合同、国家队信息。核验新版输出除授权的报告文字外与S2数值相同，原始S2输出不覆盖。
3. **选择界面。** 一线队与职业青年队文案隔离，青训分支保持原义；低状态说明覆盖年轻一线队。手机隐藏description，故PUSH伤病/身体代价、TEAM_FIRST关系收益和首发取舍必须在可见短文案表达。不得引入大卡片或扩大字体来容纳。现有390宽策略区115px/按钮66px供比较，允许必要换行但不能明显膨胀；基线 `/tmp/PSU-20260912-coordinator/ui-baseline.json` 及 before-390.png / before-1280.png。
4. **版本。** package与package-lock根版本改1.9.0，Settings既有版本信息中增加package同源游戏版本；不可批量替换依赖内部1.1.0，不变更SAVE/DATA12/12。
5. **旧档。** 依S1 30份raw来源清单直接独立加载，覆盖计划/事件/READY公开合法后继及重载，报告/市场/退休内容保护，v11备份不丢失。无效夹具应保留失败并解释；不先重存清洗以绕过旧档校验。旧测试修改逐条有依据，保留原冻样和生成保护。
6. **恢复实际后继。** 使用S2 mature/PUSH与veteran-sharp/PUSH的final状态，分别通过真实页面进入第5个半年并核验恢复接替，报告和重载正确。这是新增后继，不改S2的48窗口径。若事件/后果改变恢复资格，以最终执行状态为准并完整记录；不得手改状态强造通过。
7. **页面。** 隔离Playwright，1280×720和390×844主验，320×568及430宽度抽查。实际点击三策略→事件→报告→回看/重载/下一计划，检查首屏、换行、底栏遮挡、控制台和防重复。实际截图需目视，不以DOM判定代替。截图、临时脚本放/tmp，项目证据索引记录路径和SHA。不得读取用户真实浏览器存档。统筹使用4289服务，请使用其他空闲端口并只关闭自己的服务。
8. **工程。** 定向、最终全量test:run/typecheck/build。精确候选源码未变可引用刚完成的检查；源码再改时补受影响验证。大包警告如实记录，不扩到打包优化。
9. 证据写S3目录，更新主规格和执行台账，提交最小交付摘要、截图/原始raw/脚本入口、源码变更清单和保护结果。完成后主动通知统筹并停在S3验收；不提交、推送或部署，不自行进入S4。

用户亲自试用为可选，验收由统筹负责，不要求用户转交消息或逐项确认；不伪称用户已试用。
