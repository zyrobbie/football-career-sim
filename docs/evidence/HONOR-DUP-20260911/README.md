# HONOR-DUP-01：转会后的重复冠军修复

2026-09-11。用户截图是科莫2035冬同年意大利杯两枚，没有提供用户raw。本修复用明确构造的固定输入证明同类缺陷，不声称复演用户确切生涯。

## 原因与最小修复

现队和上半年旧队各用club.id独立计算赛季结果，随后把两队冠军拼接。两队可能同赛季同赛事均夺冠，荣誉id因clubId不同而不同，但label相同；原累计直接计数且声誉重复。

固定seed `honor-dup-42`，2026起始、W19、现队科莫/前队国际米兰：旧结果两意甲、两意大利杯、一欧冠，声誉+9。修复后意甲/意大利杯各一次，保留前队欧冠，声誉+6。未更改随机流或获奖概率。

唯一键限定CLUB冠军type＋规范化的competitionLabel＋seasonLabel（仅Unicode标准化及两端空格，不猜测赛事别名），按原数组稳定保留首条，现有生成顺序为现队优先。不同赛季、不同赛事、个人/国家队荣誉保留；身份缺失记录不擅自删除。

旧档：共用只读规范化覆盖报告（含无history旧报告）、总荣誉、俱乐部明细、荣誉室、退休统计和PNG。先对全集合处理再筛球队，不让两队分别再次计数。raw/history及已保存报告的原数组、历史声誉不写回或回扣，SAVE/DATA12/12不变。这是对不可能重复荣誉的确定性展示修正，不能重建真实冠军归属。

## 验证

新增8项有意义回归；最初7项在修复前5失败/2通过，red原输出保留。最后定向及73文件730项全量、typecheck/build通过。旧“重返俱乐部两赛季”测试的窗口8/16都默认2031，现明确2030/2034，仍断言count2，未放宽期望。

390×844/1280×720隔离Chromium实际继续、履历导航、刷新、退休导出按钮：报告三项各一次，荣誉室/俱乐部明细一致；report-only也正确。读取、页面和导出前后原存档字节保持，console无错误警告。两张真实PNG为2360×2496、2360×2958，均目视确认杯赛×1、前队仅欧冠。首张移动报告截图被底栏截住，补正常居中滚动完整图；不声称任意滚动位置无遮挡。

实际截图/PNG及原始日志位于本地`/tmp/HONOR-DUP-20260911/`，未将raw或截图纳入发布包。二维码生成/样式源码未变，沿用上一版已验收结论；本次未新增扫码测试。真实手机、微信/Safari、相册保存和用户确切存档未测试。864.09kB构建大包警告保留。

## 发布边界与回退

独立候选基于远端`87d4e0ec93d4ea0d92de173b0de494968cfac1ed`。原工作区HEAD`9de71f9`包含未获推送许可的R2文档，原样保留且不在本提交祖先链中。只提交5生产文件、2测试文件及本文；不推旧文档，不改版本1.1.0、资源、依赖、样式或存档结构。

发布前核对远端仍为该基线、建立独立注释标签`rollback/honor-dup-20260911-before`并临时副本revert验证。按统筹候选放行及用户授权普通推送，必须等待精确SHA的Pages成功并核验普通URL产物/匿名页面，不能仅以本文或push成功声明上线。最终提交SHA、CI和线上结果另存本地release-result.json并在交付消息报告，避免为记录制造尾部文档提交。

需要真正回退时须另获授权：在干净独立checkout确认本修复相关路径没有后续改动，`git revert --no-edit <本修复产品提交SHA>`，运行全量/typecheck/build后普通push main，等待Pages并核验线上。遇到冲突或漂移即停，不reset/force，不回退本机用户存档。
