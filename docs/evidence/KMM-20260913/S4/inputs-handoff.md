# S4 输入预备（尚未执行长生涯）

统筹在开发员实施S3时，按S0 `long-career-plan.json` 的既定100种子和11位置清单生成开局输入。此项只是并行准备，不代表S4已执行或验收。

原始输入：`outputs/KMM-20260913/coordinator/S4-inputs/initial-raws.json`，100个独特raw/seed，ST10个、其余位置各9个。详见本目录 `coordinator-input-manifest.json`。这是固定测试seed/年份后通过身份、位置、优先级、偏好和确认球员公开流程生成的真实保存envelope，全部停在ACADEMY_OFFERS，history为空；不是自然玩家历史采集。

固定seed与2026年份在生成player/offer之前设定；后续每次公开操作都校验保存并重载全状态相等。100条原档须直接加载，不改年龄、能力、潜力或球队。所有ON/OFF及两选择政策分支从同一raw出发。偏好统一CONDITIONAL/意大利，其结果不能外推为所有偏好政策或联赛分布。

生成脚本和实际源码SHA与输入并存。生成时初始创建、球员、存档和Store仍与S1一致，S3中的模板目录/摘要组件已有局部变化；这些输入尚未生成比赛时刻，不用它们证明S1与S3模板等价。S4正式跑前核对原档SHA、13/13及入口合法性，锁定其余选择政策，再执行既定100种子长生涯配对。

不要覆盖此输入文件、重置种子、重复统计成新增100样本。后续新增运行写各批新目录；每步记录精简状态摘要与哈希、必要检查点，避免重复保存海量完整历史。
