# ASTRA-KMM-S1 / KM-02 三模板候选

状态：待统筹验收。仅三模板闭环，不是V2.5完整发布。package2.0.0，SAVE/DATA13/13；HEAD7421b442983d27bc17f535cf741a7f065b1ee87e。源码未提交；本批恢复执行后未推送部署。

用户经统筹新确认授权比赛结算、V13备份恢复和局部页面补修。先前两次自动审批拒绝保留在approval-block.md；新确认后升级审批已允许修改。未重启S0。

## 入口

- [验证范围](case-plan.md)、[断言及中断点](assertion-map.md)
- [页面审查](page-review.md)、[尺寸/截图SHA](page-summary.json)
- [候选源码SHA](candidate-source-sha.json)、[保护差异](protection-candidate.json)
- [必需测试输入](required-test-inputs.json)：最终提交必须携带这些原始输入，不可排除证据后令CI缺文件。
- review-fixtures/：五个明确构造匿名样本，每个Pending/Result/Report三份真实保存envelope，可独立加载。来自S0 raw经当前公开恢复，再修改seed/位置熟练度/球队合同与故事字段；逐字段原始、构造后数据及真实动作见 `/tmp/KMM-20260913-S1/pages-r4/results.json`。不是自然生涯采集。
- 本机大证据 `/tmp/KMM-20260913-S1/`，归档见下方产物索引。页面脚本保留在本目录，新输出mkdir独占，已有输出复跑会拒绝覆盖；使用时改为一个新输出目录。旧S0采集器未运行/修改。

## 实现

类型与三模板目录；纯准备路径复用（年龄资格→职业准备→维护/恢复）；固定独立随机源及冻结选项/结果；进球助攻黄牌先进入原统计，评分修正最后取整。选择前不落模拟收益，结果保存后再次确认结算。V13严格Pending校验、原始V12独立备份、V11保护、写入读回及恢复；报告和履历仅展示保存快照。只在ST/LW/RW、CAM/LM/RM、CB合法池内触发，其他位置暂无模板，留扩池阶段。

报告与履历快照窗口/球队/内容一致性校验；canonical键按代码点排序，不依赖locale。三按钮54px触区，13/12/11px原字号，统一展开说明；黄牌风险默认可见。结果页明确“完成后计入”，报告显示“已包含”。没有改市场、训练维护参数或原成长公式。

正式测试仅适配V13字段、新phase公共操作和真实Storage。全生涯聚合复用同轮已完成36例结果，消除重复36条执行，不增加超时、不降低断言。旧测试人为移动到退休窗口时同时移动构造快照身份；没有改冻结输入。所有首次失败日志保留。

## 验证与边界

关键专项33项，包括8例OFF冻结业务全等、7例准备/维护/恢复/后果/22岁/末期闭环、27种构造结果注入、错误上下文、写入失败、扩目录文案后旧记录不变。全量与typecheck最终结果见commands.json；build-final.log成功（931.73kB大包警告保留）。后续测试夹具修改不改变生产bundle身份。

页面五组：390×844单刀、402×874组织/31岁、1280×720中卫/34岁、320×568恢复、430×932中卫；四主题。正向/普通/负向结果均有。五次半年结算（不是长期观察）；35次中心命中记录，6次刷新恢复按钮真实点击另计。新27张支持图全部目视；补充初轮2张亦目视但不作为320底部通过依据。

真实手机、其他浏览器、线上、100种子/1000普通窗口、12/40模板、S2人工选择体验均未执行。首秀样本历史为空是明确构造；固定非首秀命中/未命中另有单测。未把小样本当触发分布或平衡证据。

统筹仍需独立验收S1，包括保存故障矩阵与实际页面；之后再派S2/扩池。完整V2.5及发布尚未完成。

归档：`outputs/KMM-20260913-S1/local-evidence.tar.gz`，SHA/字节数见artifact-manifest.json。4311/4312本批服务已关闭；浏览器context与进程均由脚本finally关闭。最终全量76文件780项通过；typecheck退出0。S1目录保留所有失败与修复后日志，不把首次失败覆盖为通过。
