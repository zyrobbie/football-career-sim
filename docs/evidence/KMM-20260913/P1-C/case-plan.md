# P1-C运行前范围锁定

只改keyMatchMoments选择池；先合法位置，历史倒序最近四次实际Moment，空窗不清除；池空逐条解除最旧限制。参数、schema、版本不变。已冻结Pending不重选。

合成：每组20条固定seed `kmm-p1c-{位置}-{0..19}`，每条24次；窗号从4起每次+2，间隔一个无Moment窗。独立测试少于5、单一、空池与换位置。合成2400选择不计自然生涯。

自然：S4原100raw与原policy.md字节相同，100OFF/100ADAPT/100ROTATE，9复演，1000动作上限不变。运行前audit-policy-lock.json固定脚本/源码/政策/输入。只新增近期四次断言、纠正合同资格记录及5路径保存钩子；不改非Moment决策。

预选ADAPT seed000/003/006/008/010全部实际Pending/Result/READY/报告通过写入钩子gzip保存，不事后捏造。每条至少20次要求不足时报告并按已定清单有界补，不延迟退休。代表页面交统筹直接读这些原档，不重复旧截图矩阵。

旧后继：S1既有正式测试；P1A/P1B原Pending及当时Result/报告从原页面归档提取，独立空Storage直接加载，公开choose/finish后相等，再用真实写入钩子捕获READY重新加载完成，旧history不改。测试输入单独标注提取来源，旧文件只读。

784既有全量加本批新增测试，typecheck/build最终候选各一次。大运行独立命令，禁止把一次audit单测算309个新增单元测试。停止于KM09待验收。
