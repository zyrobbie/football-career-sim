# V2.5 发布统筹复核

状态：补正部署已成功，统筹线上互动复核通过；最终文档收口及开发员V12迁移证据见本批发布记录。

产品提交 `404572eb6e557bf050b337a513c88c693cf5b7b7`；标签 `v2.5.0` 指向产品提交，`rollback/pre-kmm-v2.5.0` 指向 `7421b442983d27bc17f535cf741a7f065b1ee87e`。统筹独立核对产品165项变更均属于已批准166路径（其中一份原有夹具无变更）。

## CI 补正范围

[首次工作流](https://github.com/zyrobbie/football-career-sim/actions/runs/34745481895) 的两次运行均为795通过、1失败，失败原因不同：

- attempt1：完整生涯双重演 seed6-1 耗时5211ms超过默认5000ms，另一个重演4789ms接近阈值；未报告状态不一致。只给完整生涯和双重演两组 `it.each` 显式30000ms预算，保留全部断言、36样本、6重演及动作上限，不全局放宽超时。
- attempt2：国家队退役确认测试创建职业报告夹具失败。统筹发现该测试未安装隔离存储，夹具随机触发新时刻时无法满足持久化保护。要求开发员确证并安装/清理测试存储，固定初始条件且明确覆盖实际触发时刻的路径，不用不触发种子规避；禁止放松生产存档守卫。

原失败日志保留于 `outputs/KMM-20260913/coordinator/publish/ci-attempt1.log` 与 `ci-attempt2.log`。不以不断重跑代替补正，不移动已推送标签。补正提交与最终线上验证另记，生产功能仍以产品提交为基准。

## 补正已验收

`b08ac9e014c8facd515a6ef00b165e21ac00b20f` 仅修改上述两个测试文件。固定种子 `ci-confirmation-77` 无存储时停在READY并提示无法保存，安装存储后到达职业报告及旧阶段；新测试显式断言报告中确有时刻且与历史一致。原国家队取消/确认及俱乐部状态断言保留；其原有34岁构造相应同步时刻窗口。未放松生产守卫。

隔离候选 `/tmp/KMM-20260913-ci-fix` 全量78文件/796项及typecheck通过。统筹逐一核对937份跟踪文件与该补正提交完全一致，结果存于 `outputs/KMM-20260913/coordinator/publish/ci-fix-source-check.json`。[补正部署](https://github.com/zyrobbie/football-career-sim/actions/runs/34746326886)已由统筹通过gh独立确认completed/success。

## 统筹线上验收

实际访问 https://footballcareer.zyrobbie.site/ ，全新隔离Chromium会话390×844加载P1-C自然CAM种子003/W24的原始Pending。通过真实按钮进行说明展开/折叠、选择B、刷新后继续、完成半年、履历展开/返回、设置；8次带中心命中检查的点击另加2次继续按钮。结果和报告完整GameState均与本地已捕获自然路径相等；刷新不重抽，历史24→25，回看不重演，页面无异常。真实在线资源为 `index-wfvcBMHG.js`、`index-B9zR8K1S.css`，设置显示2.5.0及13/13。

逐张目视5张：选择、助攻结果、半年报告、履历、版本。选择/结果短句、收益和风险层级清楚，未出现年龄布局膨胀或遮挡，允许正常滚动。截图、原始输入/输出、动作记录和脚本保留 `outputs/KMM-20260913/coordinator/publish/ONLINE-R2/` 及同级 `online-review.cjs`。这是AI实际页面验收，不是真人游戏研究。

首次ONLINE脚本已走完互动，在读取设置页时因两个main元素导致选择器歧义；未发现产品故障。保留首次记录，改用已观察的 `main.settings-screen` 后在新会话完整复演成功，未改生产。原输入和冻结证据未覆盖。

V12迁移/备份、线上资源身份对照及最终文档提交由开发员记录，统筹继续核对。真实手机、其他浏览器及V13无损降级未执行，不以当前Chromium验证代替。
