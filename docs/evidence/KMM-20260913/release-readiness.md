# V2.5 发布收口要求（准备清单，尚未执行）

用户已授权完整升级、推送 main 和自动部署；最终候选通过统筹验收后直接执行，无须再次请求同一授权。本文件不代表当前候选已发布。

## 候选范围

- 最终40模板、每组8；近期四次避重复与退化，旧Pending/Result/READY及报告快照保持；package与lock为2.5.0，SAVE/DATA保持13/13。
- 精确列出本升级生产/测试/必要文档，按清单stage。不得`git add .`、清理旧未跟踪文件、重置别人的改动或修改先前冻结证据。
- 默认CI当前必要输入以P1-C/required-inputs.json的defaultTargeted为准，共48个路径，统筹已逐一核对SHA；包含早期17个和P1-A/B Pending、P1-C旧Result/报告及索引。新增测试若引入输入必须继续合并。审计脚本使用的独立outputs不能默默变成默认CI依赖。
- 300生涯轨迹、截图等大证据本地归档并记录SHA，按实际需要选择简明索引入库；不能只推代码遗漏所依赖测试原档。

## 发布前验证

- 最终呈现补清楚一个语义：结果页与报告/履历中保留的比分是机会发生时的上下文，显示时加“当时”短标签，避免进球结果下仍见原比分而误解。只改显示标签，不更新历史快照或模拟终场比分；保持字号/间距并实际查看320和主手机宽的进球结果及回看。此项待P1-R派发，当前未实施。
- 最终候选全量、typecheck、build与源码SHA一致；布局以实际图片/操作核对，不能以DOM和绿色测试替代目视。
- 在隔离目录准备只包含已跟踪基线及明确发布文件的干净候选，执行默认CI测试/构建，验证没有依赖工作区旧未跟踪材料。隔离目录可复用本地已安装依赖；这不能声称新联网npm ci已跑。
- 建立V2.0回退点7421b442983d27bc17f535cf741a7f065b1ee87e的可识别tag或等价明确记录。保留完整变更归属与可重演回退方法，在隔离副本验证；不在当前主分支试回滚。

## 存档边界

V13保留独立V12原始备份；代码回退不等于V13可由旧客户端读取。回退前先保留当前V13原档，旧版本恢复V12备份会丢失升级后的进度。禁止声称实现了无损13→12降级，也不得自动抹除新档来让旧客户端通过。

## 已授权发布与线上证据

明确候选验收后提交、推送main，确认远端SHA与GitHub Pages工作流成功，并核对部署产物版本/资源身份。线上用全新隔离匿名会话验证入口及关键时刻选择→结果→报告/保存恢复，保留必要证据；不读改用户浏览器真实存档。最后同步任务状态、已发布commit与回退点、线上实测范围和未测限制。
