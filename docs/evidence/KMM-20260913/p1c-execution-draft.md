# P1-C 执行预案（P1-B验收后由统筹放行）

当前是准备稿，不表示P1-C已经执行。放行后只改近期模板选择器、对应测试与必要审计工具；40目录数值/文案保持。package暂2.0.0，SAVE/DATA13/13。

1. 从历史倒序读取最近四次实际Moment（跨无Moment窗口），先取得当前位置合法目录，再排除近期四ID。若候选空，从最旧记忆依次解除限制，仍有替代项时不选最近一次；永远不能借用不合法位置模板。候选只有1个也须正常继续。不要新加存档字段、迁移或规则版本。
2. 新算法只影响尚未生成的Moment；旧S1、P1-A、P1-B的Pending/Result/READY和正式历史保持当时快照。公开选择→保存刷新→完成后对原后继/快照一致，不为了通过而更新旧raw。对P1-B至少补已生成Pending以及同一条实际选择所得Result/READY的回放链；记录哪些是现有原档、哪些是由其新产生。
3. 100条24次合成抽取轨迹，五组各20；完整覆盖组内8，逐条≥7/8覆盖至少90%，最高单模板≤6；有≥5合法候选时不能重复最近四次。覆盖空窗口、位置变化、少于5池与只1池的退化；保留合法候选、最近记录、实际选取，指标不通过不得改种子抹反例。
4. 使用S4同100raw、同固定政策新跑100OFF+100ADAPT+100ROTATE及9确定性复演。沿用原原始政策的训练/市场/事件行为和统计纠正后的合同资格分桶；原S4结果不改。只更新审计候选身份、独立输出路径及为本批新增的覆盖/快照记录。保持全部合法退休/后果/保存/历史不重复断言，至少1000普通窗口，按位置/政策分析球助/评分/荣誉/出场/职业长度、均值中位数及直接/总影响。8%/10%是需解释的观察线，不能擅改生产数值。
5. 五条自然路径及额外Pending捕获按p1-long-career-review-plan.md，提前固定seed而非事后挑选。逐窗表带场景、实际选项/理由画像、结果和间隔，交统筹审阅。为代表路径实际页面操作提供原始Pending和精确后继，默认不需要复拍全部旧截图。AI审阅和自动点击不是人类研究，20–40秒不虚报。
6. 本批新引擎行为跑必要定向/全量/typecheck/build，严查旧Pending输入，不把“旧档可加载”代替“合法后继和保存重载可继续”。输入清单增加本批默认CI所需文件；大型显式审计仍独立命令，不能误计默认测试。
7. 新证据只入P1-C和独立outputs，完整README、候选/保护/命令/矩阵/自然300分析/5路径输入与复跑方法。交付KM09待统筹验收后停；不提前发布，未测真机/其他浏览器照实列。完成后P1-R升级2.5.0与最终发布按既有授权执行。
