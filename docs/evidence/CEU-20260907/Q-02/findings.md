# 待统筹处理的导出视觉问题

本批只验收，生产保持原样。以下为当前真实匿名终点直接通过产品生成PNG的反例，不是构造数据，也不是页面截图代替导出。已关闭T/M/F问题不因此重开。

## Q02-V01：移动导出混合桌面表头和移动履历布局（阻塞该视觉子项）

- 输入：`desktop-final-raw.json`，来自本批1280实际UI链W55/AGE_LIMIT，56条history；SHA见raw-sources。390或375独立context原字节注入→继续生涯→正常滚动→真实按钮“保存我的职业生涯”。无先重存、无内部绘制调用。
- 实际：页面本身正确采用移动卡片；实际PNG `retirement390-export.png` / `retirement375-export.png` 为2206×5411，两图SHA完全相同。导出显示8列桌面表头，但每行仍保留移动端的多行及显式列位置。例如首队“青年队”在左侧下一行，“77”处于“队伍”标题附近而非“出场”下；“场/球/助/峰”移动伪标签同时出现。各队数据存在，但表头和数据不能按列可靠对读，空间明显拉长。
- 对照：同raw、1280生成 `desktop-export.png` 2360×4722，列对应正常。见mobile-export-detail-top与export-detail-top/middle。状态、summary相同，不能用字段数值正确掩盖导出排版错误。
- 静态依据：main.css 7684附近导出覆盖恢复桌面grid模板，却没有重置8050–8098移动规则赋给子元素的grid-row/grid-column和伪标签。此为基于现有CSS与产物的成因判断，未做生产修改试验。
- 最小建议：另行授权仅隔离导出克隆的子元素布局/伪标签及必要字体，使固定导出布局不继承移动定位；以同raw两主尺寸实际PNG核验。不要改正常移动页、数据、导出资源或二维码。由统筹决定是否补修及任务边界。

## Q02-V02：桌面荣誉图标内容过小，PNG中呈窄条（次要视觉问题，待统筹判定）

- 同一桌面退休页的荣誉室徽章图片自然尺寸150×150，但实际内容盒为6×24，见final-observation.json。页面中图标很小；export-detail-bottom.png中对应图标呈窄条，俱乐部履历中的徽章则完整。
- 名称、次数及8项荣誉统计存在，无资源加载错误。不能声称所有徽章视觉均通过。
- main.css 7398对`.retirement-honors__list span`施加水平9px padding，会同时命中24px的嵌套honor-badge；这是宽度不足的直接样式线索。PNG与页面对object-fit的呈现差异未进一步隔离，不推断所有渲染器行为。
- 最小建议：另行授权时限定检查该嵌套选择器，保证徽章内容盒尺寸；同原raw对照页面/实际PNG。与Q02-V01分开验证，不扩大荣誉规则或资源改版。

没有存档损坏、重复结算、组合流程卡住或二维码错误证据；这些独立检查已完成。Q-02交付状态为待验收（上述导出视觉子项未通过），不能宣布全部页面/图片验收通过。
