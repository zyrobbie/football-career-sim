# V1.9 回退准备（尚未发布）

回退基线为 `2570017bd4da05aa2c1db11b9ca4e70c98d40427`，拟定标签 `rollback/psu-v1.9-before`。只读核对远端main仍为该值，标签当前不存在；本批不创建/推送标签。发布授权后，在main无外部新提交且候选SHA全匹配时先给此基线打标签并推送，再提交精确候选/推送main。main push会自动触发现有GitHub Pages工作流。

正常回退使用 `git revert <实际发布提交>`，保留可审计历史，正常push触发CI重新构建/发布。不得reset/force-push，不在用户脏工作区盲目revert。隔离干净工作树中从实际发布HEAD新建修复分支，确认revert无冲突且代码、package/lock、资源树恢复到基线，再由统筹按当时授权发布。若后续另有提交，逐一核实依赖与冲突，不直接执行本次演练命令。

本批仅在/tmp隔离候选副本以临时提交演练git revert，对照git tree完整一致，不修改用户仓库、用户存档或线上资源。临时commit仅用于树级回退验证，不是正式发布提交；实际发布SHA由统筹确认。

本升级SAVE/DATA12/12不变；代码回退不等于撤销已经发生的球员状态、比赛或存档收益。不得宣称游戏历程可倒退；不覆盖、降级或删除本地存档。v11原文备份保护仍保留。必要时先由用户导出/备份真实生涯，真实浏览器数据不由本次脚本访问。

发布仍需：统筹连续体验结论、精确候选批准、标签/commit/CI/线上资产核验。大包/真实设备限制不得在发布记录中消失。
