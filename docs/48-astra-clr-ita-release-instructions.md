# ASTRA-CLR-ITA 发布与回退点指令

2026-09-11。用户明确要求：指挥「开发员 Astra 轻」完成推送部署，并保留回退点。本指令授权将已验收的意甲批次推送 main，触发既有 GitHub Pages 线上部署，直至验证完成；不再等待重复发布确认。文档46此前“未授权发布”是历史状态，本指令及用户新授权优先。英超E阶段不实施。

## 执行范围与基线

工作目录 `/Users/zhihu/Documents/足球生涯游戏`；当前基线 `16b8ddf2c14dbc36f7832a58d96ab18da1b53825`，package1.1.0，SAVE/DATA12/12；origin `https://github.com/zyrobbie/football-career-sim.git`。主台账文档46、统筹验收 `docs/evidence/CLR-20260911/ITA/review/README.md` 为准。20枚SVG、清单及2份直接测试已验收。禁止重新设计/变更CSS、业务、存档或依赖。不得改英超、中国及其他队徽。用户常用存档、下载原件、旧CEU/outputs和本轮冻结证据保持。

## R-ITA-01：发布前检查与可用回退点

1. 核对HEAD、分支、索引、diff、远端main和当前部署。已有未跟踪文件数量较多，禁止git add .、git clean、reset --hard或force push。先运行统筹review/audit.py核验（其HEAD断言适用于发布前）；原A/pilot/final的阶段快照不覆盖。发现远端前进或意外源码变更，交统筹处理，不猜测抹平。
2. 生成精确候选清单：20个意甲SVG、clubCrests.ts、2个相关测试、本轮文档46/47/48及必要的精简发布记录。按文件逐项暂存；不把旧CEU、outputs、大量截图、匿名raw、临时脚本、Git备份包或下载目录整体推送。完整本地证据可以保留本地，发布记录明确哪些证据仅本地可见。不得改写旧证据。同步纠正文档46当前状态和旧盘点的时间限定。
3. 以实际已部署且可恢复的main基线创建带说明的回退tag，首选 `rollback/clr-ita-20260911-before`。验证其提交与预期基线一致；若已有同名同提交可复用，异提交则使用新唯一名字，不覆盖。推送这个tag至origin，使用远端peeled commit核验。额外保存精简baseline.json：基线SHA/树、原部署ID、原8枚SVG/清单SHA、版本。回退不能只保留散落旧图。
4. 对精确候选树做干净目录构建/测试，优先临时独立checkout/worktree，使用现有依赖避免无谓安装；测试中应包含全部正式已跟踪测试（预期72文件722项，数量差异先解释）。运行full/typecheck/build并记录候选身份，确认dist20SVG字节一致；工程测试通过不代替已验收的视觉证据。保护现有工作目录改动，不混入未发布文件。

## R-ITA-02：提交、main推送与部署

1. 提交已核验候选树，记录准确发布提交SHA和文件清单；普通fast-forward推送到origin/main。首次发布提交不能有skip ci。不改写历史，不顺带推tag全集。
2. 等待与该发布SHA对应的 Deploy GitHub Pages workflow 完成，保留run URL/ID、build/deploy状态及线上URL。push成功不等于部署成功；如失败，先定位日志，只在本次发布范围做最小修复或交统筹，不动游戏规则。必要时可重新运行对应workflow。
3. 对线上普通URL的20枚意甲SVG逐份HTTP/字节SHA核对，确认与本轮源文件/构建一致；检查返回的页面引用正确的新JS/CSS，非目标代表资源和英超短标清单保持。首次不靠随机query绕过旧缓存，若发现缓存旧图，有限等待重验并记录普通URL与强制刷新的区别；不要宣称所有用户缓存已刷新。
4. 隔离浏览器打开线上入口、继续到可见的正常产品页面，核对URL/title、正文、console与实际截图。使用独立临时context，禁止清空用户线上存档。为验证新图可用，允许最小隔离匿名展示夹具/临时只读验收页，但不得将构造数据或测试入口加入生产。新会话及已打开页面的普通刷新检查要真实记录；线上全生涯/真机/相册未做则保留限制。

## R-ITA-03：回退方法、文档收口与交付

1. `docs/evidence/CLR-20260911/release/README.md`记录：发布SHA、回退tag及完整基线SHA、远端核验、部署run、20线上SHA、候选测试、源保护、实际浏览器范围、未测项。另给 `rollback.md`，写明以本次发布提交为单位执行普通git revert并推main重新部署的精确命令；tag用于确认原树/旧8图和原清单，新增12图随revert移除。不得用reset/force push恢复，不触碰存档。回退演练在临时副本验证生产树恢复原基线，不实际回退线上。
2. 把文档46标为“意甲已上线，英超未开始”，保留主产品版本1.1.0和存档12/12，明确资产批次由Git提交标识。若需提交部署后的文档结论，可单独做纯文档 `[skip ci]` 提交并推main，分别列出main文档HEAD与真正已部署代码SHA，不把skip提交视为再次部署。
3. 完成后关闭自己的测试服务和临时浏览器；停止，不进入E。直接将最终交付发送给本统筹任务，附回退点、发布与部署、线上核验和文档路径。不创建代理。不要只停在“等待用户转交/允许发布”。
