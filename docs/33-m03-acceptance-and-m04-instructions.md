# M-03统筹验收与M-04交接

2026-09-08，Codex。原交付和冻结证据保留；本文件记录统筹后续结论。

## 1. 验收结论

**M-03通过，MR-03关闭，主台账M-03标为已完成。M-04未开始，待用户转交指令。** 当前没有发现阻塞本批验收的问题。此结论覆盖当前求职方向、保存、生效提示及市场恢复；整体市场专项仍须M-04综合验收，F/Q/R未启动。

认可实现：只修改两个player偏好字段，draft保留开局；有效值相同不写盘、不抽随机；原市场报价/选择/谈判不变，真正新市场才用新方向。已生成状态恢复先于新开机会判断，空市场与到期0–3外部同样复用，已签约/到队及下一计划不重新激活旧市场。旧回看状态按明确phase/W/H/选择/decision恢复；回看是非持久视图，报告只显示返回动作，正常开局回退和职业推进仍有回归覆盖。

MR-05保持关闭。未修改市场生成参数、存档版本/迁移、训练、球队数据、依赖；12/12保持。没有引入新的市场ID或刷新机制，F报告流程合并仍未实施。

## 2. 独立核验

- 审查gameStore.ts增量、marketContext/careerPreferences共享判定、编辑器及App/导航/报告改动，核对旧状态恢复、phase允许表、字段原子更新与生成调用边界。组件检查采用frontend-testing-debugging及React最佳实践的局部表单/事件驱动保存原则，未做额外重构。
- 独立定向15文件197项通过、typecheck通过。含8份原市场envelope、7条R1稀缺签约分支、8组旧市场phase恢复、6组实际签约后继的旧phase恢复、方向生效与正常下一半年。
- 独立核验92项交付产物SHA/字节、650项既有文件，其中635项未变，15项声明变更含1项构建缓存；新增4项源码/测试after哈希匹配。457项此前冻结证据及outputs逐字节保持。
- 全量65文件510项及build通过，采用已核验哈希的最终r2原日志，本轮未重复全量/build。包体857.65kB、>500kB警告保留，未扩大范围优化包体。
- HEAD保持3f290407a299790665b58c4bc7c2bf8455d24bbd。本轮未改生产、未提交推送部署、未创建代理。

## 3. 页面复核

环境为`http://127.0.0.1:4187/`、独立临时Chromium context；Browser plugin not available，按技能采用现有Playwright，无新依赖或用户profile。沙箱首次启动Chromium遇到MachPort权限拒绝，在进入页面之前失败；取得扩大权限后成功复跑。服务器本轮启动并已停止。

复跑仅将交付脚本的输出E改到M-03-review新目录，来源B与断言不变，保留原产物。流程：原始匿名市场/计划恢复→编辑/取消/保存/刷新→只读回看→返回/切导航/换生涯。

| 检查 | 结论及实证 |
| --- | --- |
| 页面身份/内容/遮罩 | 本地URL及“上场”title一致，编辑器、报告和实际操作定位正常；无空白页、无Vite错误遮罩 |
| 主流程 | 四视口×普通市场/计划共8组：取消/未存刷新不写偏好，保存刷新保持，连续点击不重复修改，回看与返回状态相等，导航/刷新清回看 |
| 补查 | 四视口35岁训练/联赛上限/换生涯共4组；1280/390×到期外部0–3共8组，原始写盘字段相等；补查12组完成 |
| 控制台 | 主流程与补查error均为空，补查warning为空；原主脚本未收集warning，不扩称全部警告皆无 |
| 截图 | 新生成36张，与交付对应截图全部逐字节一致；本轮实际查看430保存、320保存、390返回、1280首屏四张代表视口原图，其余沿用相同原图的交付查看记录，不称36张均独立目视验收 |
| 移动按钮 | 320/390/430正常滚动后保存/返回中心hit test命中，真实点击成功。430部分滚动位置会被固定底栏盖到，滚动可完整露出，接受为非阻塞既有布局限制；不承诺任意滚动位置无遮挡 |

代码、存档、store和页面证据分别成立，模拟视口不等于真实设备。真实手机、Safari/Firefox、线上、旧客户端读v12均未执行。截图与JSON见[独立复核证据](evidence/CEU-20260907/M-03-review/README.md)。

## 4. ASTRA-M04完整指令（待用户转交）

建议沿用Astra轻度做验收；发现共享状态/恢复的具体缺陷交统筹另定补修，不边验收边扩功能。

```text
你执行《上场》CEU-20260907的M-04市场专项综合验收，使用GPT-6 Astra轻度，由Codex统筹复核。
项目：/Users/zhihu/Documents/足球生涯游戏。
本次用户转交授权仅M-04：汇总并验证市场专项端到端契约、有限分布/长期路径和旧存档恢复，完善验收台账。M-01至M-03已验收，MR-03/MR-05关闭。不得进入F/Q/R，不提交、推送、部署，不创建代理。

一、前置及范围
读取适用AGENTS.md，docs/20第4/5/6.2/7/10节、docs/21市场栏、docs/23第2节、docs/30–33，以及M-01/M-02/M-02-R1/M-03与相关统筹复核索引。
核对HEAD（预期3f290407a299790665b58c4bc7c2bf8455d24bbd）、12/12版本及git status，保存本批前哈希，保护全部已有未提交源码、冻结证据及outputs。不得使用用户真实存档或浏览器profile。
授权后统一主台账顶部、M-04行和第10节为进行中。生产行为默认不修改，仅测试、证据与文档；发现具体问题记录复现、影响和归属，交统筹决定补修，不自行调公式、放宽约束或写新持久字段。

二、验收总表
在docs/21建立M专项总表，逐条映射docs/20第6.2节：资格、三方向组合、确定性、续约另计、救济严格改善、未成年/年龄/开启门槛、当前方向与开局档案、下一未生成市场生效、当前市场稳定、旧档及签约后继、实际页面。每行列输入来源、当前实现、验证层次、证据及限制，不把未执行写为通过。
已有通过的精准证据可引用并核验源码/产物哈希，无需无理由重复采集或覆盖旧输出。MR-01自然救济缺口与明确构造稀缺/撤回证据保持原标注，不重新展开完整生涯搜索。

三、有限综合验证
1. 复用E-03完整输入及100固定种子，核对三方向输出及分布，验证CONDITIONAL每窗两地各至少1、DOMESTIC国内候选足够时每窗至少2，STRONG无国内必出频率。资格与开放槽可达性引用直接测试，不以抽样频率替代。参数未改时，新输出应与M-02保存结果一致。
2. 常规/到期/救济三路径分别核对外部≤3、续约另计总≤4、费用/角色/现队排除/去重及不足回退。复用47边界、48稀缺和R1结果；不扩大成所有条件的全排列。
3. M-01原8份envelope和R1的7条稀缺原始envelope逐份在新Storage无backup直接加载，验证方向编辑、三次修改/同值无操作、回看/导航、重复/过期动作、保存加载、合法签约和下一计划。加载前不得先重存data；记录原完整报价及谈判对比。
4. 复用M-03的14组明确构造旧phase恢复，确认市场、到队、签约完成、下一计划仍各回正确状态；不能重新激活已完成市场。真正未开市场继续受原节奏/失约/年龄限制。
5. 补充有限的连续窗口观察：从已有匿名合法计划起点，用3个固定种子×3条预定方向策略，每支最多6个半年（总≤54窗），到年龄终点或预定边界即停。选择至少一类青年职业起点及一类老将起点，源不足则缩减并说明，不伪造自然史。
   策略包括持续综合考虑、持续国内、按固定窗口切换方向。使用公开训练/事件/报告/开市/签约动作，市场未开放时不强开；记录真正生成次数、方向生效、窗口/history/现金变化与最终可继续状态。小样本只证明流程和契约，不用于宣称最终平衡或提高国内概率。不同策略不要强求整条生涯一致。

四、页面与工程
1. 复用隔离本地origin和匿名档，做代表性桌面1280×720、手机视口390×844的综合流程：改方向→保存刷新→尚未生成的市场用新方向→已有报价编辑不改变报价→谈判/回看→返回→签约→下一计划。至少一条到期稀缺市场可继续，并检查生涯/球员/履历展示来源正确。
2. 430宽度固定底栏的已知滚动限制保留记录，核对正常滚动后保存/返回可点击；320宽局部换行和按钮可达可引用源码及截图身份未变的M-03独立证据。不是所有位置无遮挡，不擅自开展全站样式修复。
3. 运行本批新增定向、相关市场/store/persistence回归、全量test:run、typecheck、build。一次通过后不无理由重复。保留大包警告；浏览器控制台/实际点击/截图目视与代码测试分别记录。真实手机、其他浏览器、线上、旧客户端读取v12未执行则保留。
4. 有具体失败时保留原输出、查明产品失败或测试/主机问题，有限复跑；不能修改快照或删除检查来通过。遇到持久化/状态判定问题停止相关验收，交统筹，其他独立文档和核验可继续。

五、交付
新证据只写docs/evidence/CEU-20260907/M-04/，含验收映射、命令原始日志、连续窗口有限输入/动作/结果、旧档及状态差异、页面来源与截图、前后哈希、限制和风险；独占写入，旧证据不覆盖，正常测试只读。
更新docs/21市场总表、docs/20顶部/M-04行/第10节为待验收；必要时纠正规则文档陈旧“未实现”措辞，并明确历史时点，不改历史冻结记录。M-03已验收结论仅在有具体回归证据时重开。
交付摘要报告实际测试数量、旧档/连续窗/页面覆盖、限制、HEAD及未执行项。市场专项是否整体收口由Codex决定，执行者不自行宣布发布就绪。
完成后停止，唯一下一动作：用户转交Codex审核ASTRA-M04。F及Q/R未开始，不提交推送部署。
```
