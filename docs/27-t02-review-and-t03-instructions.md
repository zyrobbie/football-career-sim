# T-02统筹验收与T-03交接

日期：2026-09-07；统筹：Codex；HEAD：`3f290407a299790665b58c4bc7c2bf8455d24bbd`，改动未提交。

## 1. 验收结论

**T-02通过，标为已完成。TR-01关闭。保留当前首轮维护参数，允许作为T-03页面接入的稳定接口。T-03尚未授权、未发送。**

本结论是引擎、迁移与恢复验收通过，不是整项训练体验或发布验收通过。未发现阻塞T-03的实现问题。页面仍沿用旧训练卡片，需要下一批按年龄和实际规则接入，当前版本不能据此称为可发布成品。

### 本轮独立验证

| 项目 | 结果 |
| --- | --- |
| 全量测试 | `npm run test:run`，退出0，58文件／402项；13:48:41开始，14.20秒。包括迁移、年轻对照、维护配对、事件顺序与摘要持久化用例；未再单独重复定向套件 |
| 类型与构建 | `npm run typecheck`、`npm run build`退出0；主包848.00kB／gzip235.06kB，保留大于500kB警告，本批不扩展拆包 |
| 证据完整性 | T-02的58项artifact-hashes字节数及SHA全匹配；changed-files的25项after哈希全匹配；before-hashes中其余219份既有文件未变 |
| 迁移来源 | 直接加载原S0七份、老将十份、TR-01四份构造envelope，共21份；构造与真实来源分开，未用v12重演生成旧档 |
| 恢复边界 | 迁移仅升级合法输入版本；待结算焦点统一规范化；新history与执行焦点一致；旧历史、事件前缀与已生成结果保留，最终重载不再结算 |
| 事件与恢复顺序 | 实际事件45→49、46→45；延迟后果45→46、46→45；PUSH后46→43不重判；恢复窗三策略完整输出一致，事件及训练质量奖励不重复消费 |
| 年轻不变 | 冻结54组输入／324次输出，整个引擎返回值与v11完全相同；不是从当前helper重建输入 |
| 报告链 | 合同、现金上限、国家队三条hint与特殊事件共同存在时，eventSummary仍保留实际维护／恢复解释，保存重载一致 |

检查生产差异覆盖共享训练、两套引擎、衰退、训练质量、版本校验与store三入口；当前测试中的旧版本目标改动符合12版本迁移。v11历史复演已保存为正常测试发现范围之外的源码副本，当前M/F未改行为的检查仍在。没有为通过T-02顺带改变市场与流程。

本轮没有重新执行v11构造生成器或历史隔离复演；其源码、既有日志和冻结产物已核验。浏览器、模拟移动视口、真实手机、线上、v11读取v12均未执行。

## 2. 参数判断

**保留+4、−2／−3及身体衰退×0.8，不因评分变化小而放大收益。**

900次维护配对使用相同冻结输入与种子，三种计划确有不同作用和代价。35岁代表组中，相对旧BALANCED：

| 维护 | 代表组实际表现 | 判断 |
| --- | --- | --- |
| 身体维护 | 身体状态均值约+3.96，竞技通常−1.4；身体能力最终高0.2；取整前身体衰退仍有0.52248 | 有维护收益，保留竞技成本与大部分衰退 |
| 比赛状态 | 竞技通常+2.9，身体状态均值约−2.97 | 与身体维护目标不同，成本明确 |
| 心理调适 | 心理状态+2.2，竞技−1.4 | 与能力成长区分，未叠加旧适应额外收益 |

表中是整窗对照结果，不是计划卡应承诺的直接收益。伤病或表现门槛可改变个别种子的净结果；准确范围见原maintenance-pairs.json。直接准备公式已由测试单独校验。

该代表组评分差在−0.1至+0.1之间，绝大多数相同。状态变化仍可在报告及后续窗口中产生作用；不能据此宣传进球或胜率改善，也没有逐场胜负数据可支持此宣传。当前没有需要改动候选参数的证据。长期选择价值、页面能否让用户理解代价，仍在T-04／Q体验验收时收口，不将本次单窗统计称为最终全生涯平衡证明。

TR-01四份构造档在本次实际路径中均获得0直接维护收益、成本照常执行，能迁移和继续，不重算事件。真实饱和采集档仍未找到，这一事实保留；关闭的是已约定的补充验证条件。

## 3. T-03必须收口的呈现问题

1. 当前训练页面仍显示旧六项与physical默认值；接入年龄集合、BODY_CARE默认、恢复优先与饱和禁用后，页面说明才能与已上线到本地的规则一致。
2. 当前摘要含“取整前差额，已计入20下限”“不叠加旧适应心理+4”等实现术语，并可能输出六位小数。T-03将其改成用户能理解的表述，例如“身体衰退减少约0.13”“心理状态+4、竞技状态−2”，不暴露内部版本对比或隐藏潜力。仅调整新报告摘要格式和呈现，不改计算值，不重写任何旧报告。精确数据继续保留在测试证据中。
3. eventSummary现由单个段落渲染；下一批确保保存字符串的换行与长说明可读，不截断、不转放hints，不通过隐藏内容换取一屏。

主台账顶部仍遗留“T-02待授权／生产开发未开始”等旧摘要，与任务行及第10节冲突；本轮统筹直接修正文档，不为该记录问题要求生产返工。后续每次交付同时更新顶部、原子任务行和第10节。

## 4. 完整执行指令（供用户转交，待授权）

建议配置：GPT-6 Astra轻度；T-02共享接口已稳定，涉及计算或迁移失败应交统筹，不自行更改规则。

```text
你执行《上场》CEU-20260907的ASTRA-T03，配置GPT-6 Astra轻度，Codex主任务统筹验收。
工作目录：/Users/zhihu/Documents/足球生涯游戏。
只有用户明确转交本指令授权T-03后才启动；文档存在本身不是授权。由用户转交结果，不主动向其他任务发送消息。

先读适用AGENTS.md、docs/20第4/5/6.1/7/10节、docs/23第1及4节、docs/27全文、T-02/README的接口与限制。核对HEAD和git status，记录本批前哈希并保护既有改动、冻结证据和outputs；不访问用户浏览器存档。T-02已验收，参数保持+4、−2/−3、身体衰退×0.8，版本保持12/12。
收到授权后记录原话与范围，将主台账顶部、T-03行、第10节统一更新为进行中。

范围：只接入训练选项、默认值、预判说明和实际训练报告呈现；完成T-03后标待验收，停止。不得进入T-04、M、F；不提交推送部署，不创建代理，不调整依赖、引擎计算、存档迁移或全站布局。

1. 年龄使用playerAgeAtWindow，以本窗口开始确定，复用T-02共享类型和解析器。新增一个页面可复用的纯展示模型集中生成选项与可用性，不在页面和测试里复制规则。
   ≤30保留原六项、原名称/默认physical及既有行为；31—33显示attack/defense/mental/BALANCED/BODY_CARE/MENTAL_RESET；≥34只显示三种维护；31+默认BODY_CARE。37+说明衰退加快，不暗示仍能正常成长。
2. 页面初始化、切换窗口和载入旧计划时正确同步选择。已有待选focus按共享规则显示，不在挂载时执行训练或改写游戏。旧选择仍能继续；不得因UI列表缺少旧枚举而卡档。新用户不能点选已饱和的无效方案，旧档的已选值通过清楚说明和保留继续路径兼容。
3. 31+且当前任一状态<46：仅显示“本期先恢复”说明及开始按钮，收起可点击训练/策略收益卡。使用BODY_CARE+STEADY内部默认；明确当前是事件前预判，实际恢复以事件与到期后果处理后的状态为准。31—33保留常规成长说明，不承诺维护加成。事件使状态变化时，最终报告准确解释实际结果。
4. 正常状态：比赛状态目标form≥95、心理调适目标morale≥95时禁用并解释；身体维护即使fitness≥95仍可选，说明仍可减缓身体衰退。计划卡写直接准备收益、成本、上限及恢复覆盖，不承诺整窗净增加或比赛更好。三项维护不新增必填决策组；正常时保留现有职业策略。
5. 新报告显示保存的eventSummary，保留真实换行和完整说明；不重新推算收益，不改旧报告，不扩大hints，不接入F分流。按文档27第3节简化训练摘要中的技术措辞和过长小数：确需近似的小数使用“约”，准确计算不变。将这类修改限定在trainingEventSummary格式器及对应文案断言，保持全部数值回归通过；不得修改年轻原摘要。
6. 先验代表页面：桌面1280×720、移动390×844，覆盖年轻、31—33、35岁、37+、恢复、饱和、旧计划和长报告。用隔离浏览器上下文/测试存储，不接触真实用户存档。保存截图与操作步骤，检查层级、换行、遮挡、按钮可发现性、禁用理由和滚动可读性。页面没溢出不等于视觉通过；不扩大到全站CSS。无法取得实页证据则明确缺项，不称已通过。
7. 为选项/默认/预判边界和旧计划恢复写必要定向测试；重点30→31、33→34、36→37，45/46、95/100，以及跨窗口选择同步。验证按钮只提交一次当前明确选择，不能在render/effect里推进生涯。全量测试、typecheck、build通过后，不无理由重复测试。

交付：页面与必要共享展示模型/摘要文案变更、测试、docs/evidence/CEU-20260907/T-03/索引及截图/步骤/日志/哈希；更新必要产品说明。区分自动化、模拟视口和真实手机，真实手机未执行必须明确记录。
主台账顶部、T-03任务行及第10节统一标待验收；提交T-04待验清单，不自行执行T-04。汇报文件范围、测试、实际页面证据、限制和HEAD，停止交用户转Codex审核。
如需要改变T-02计算或迁移，先记录具体复现和最小建议交统筹，不能用改参数掩盖呈现问题。
```

## 5. 当前检查点

T-02已完成，TR-01关闭，首轮参数保留。T-03待用户授权转交，未发送、未启动。T-04及Q负责训练专项与整体体验最终验收；发布前另落实存档备份/回退，不把内部滚动备份自动覆盖后的结果当作持久v11回退保障。
