# F-04收口与Q-01交接

2026-09-09，Codex。保留原F-04及R1交付记录，本文记录统筹最终结论与下一批边界。

## 1. 验收决定

**ASTRA-F04-R1通过，F04-V01关闭；F-04标已完成，F专项验收收口。** T/M保持已验收，K-09、国家队资格、MR-03/MR-05保持关闭。Q-01及后续未开始，专项通过不等于升级最终验收或发布授权。

生产差异核验为`src/styles/main.css`既有`.transfer-offer-grid__environment`规则增加三条：`white-space:normal`、`overflow:visible`、`text-overflow:clip`。移除这三条后CSS哈希与补修前完全相同；TSX和其他源码/测试无变化。

1280宽度的“国内良好训练条件 · 融入较低”原容器113px、内容116px，原单行省略；现在完整显示为两行，字号仍9px。第二行仅余“低”，排版朴素但信息完整，不构成连续逐字竖排；本轮接受自然换行，不再扩大成卡片重设计。其余四视口原字号、文案、列数及价格/角色布局保持，卡片增高没有遮挡详情或确认按钮。

## 2. 独立核验

本地`http://127.0.0.1:4196`，已安装Playwright与隔离临时Chromium；Browser plugin not available。目标路径：合法已生成市场→逐卡选择→必要签约/到队→完成→下一计划→刷新恢复。

| 检查 | 结论及范围 |
| --- | --- |
| 工程 | 独立11文件181项、typecheck退出0。核验R1 build日志及源码身份后复用其成功结果；全量626复用已核验F-03，当前仅CSS差异，不重复全量/build |
| 页面身份/非空/遮罩 | 七组均通过标题、页面内容及Vite遮罩检查；仅本地4196，非线上 |
| 控制台 | 七组pageerror及console error/warning均空 |
| 五视口 | 1280×720、1366×768、390×844、320×568、430×932同一三外部报价状态；另390零外部续约、1366一外部稀缺，共七组 |
| 信息完整 | 同输入补修前后文案/全部卡片字体一致；选中/未选中环境说明均完整，实际截图核验无新增信息截断、遮挡或逐字竖排 |
| 真实交互 | 30次记录点击，按钮滚动后完整入屏且中心命中。1280/390外部签约→NONE→计划的最终game与F-04原结果完全相等；390零外部续约到计划。报价不被选择重抽，history/现金无额外半年结算；刷新/磁盘数据一致 |
| 截图 | 独立23图中21张与原R1成功图字节相同；两张签约完成图有条形渐变/颜色等截图时点差异，已与原图目视对照，内容/按钮无新缺陷，状态完全一致，未单独诊断动画成因 |
| 文件保护 | 56项R1交付产物、1462既有文件核验；既有变化仅CSS及docs20/21，1158冻结证据/outputs保持。151源码/测试中仅CSS有授权三行差异 |

实际目视九张本轮图，覆盖五视口及稀缺/完成布局；另查看原1280补修前图及两张原完成图作对照。没有声称独立目视全部23张。同输入重复不算新增自然档，30次点击不作为双击证明，双击已有F-02/F-03独立证据。

初次沙箱启动Chromium因MachPort Permission denied失败，未打开页面；升级沙箱权限后一次重试七组全部通过。这是主机限制，不计为产品失败。服务与临时浏览器已关闭；未访问用户真实存档。命令、截图、逐动作状态及保护记录见[独立索引](evidence/CEU-20260907/F-04-R1-review/README.md)。

真实手机、其他浏览器、线上、旧客户端读v12及发布回退仍未测；既有移动小字、正常滚动与859.40kB大包警告保留。375×667是主台账综合页面门槛中尚需Q-02补足的尺寸，不把本次五尺寸说成最终六尺寸全覆盖。最终是否接受设备限制由Q-04记录。

## 3. 下一阶段安排

建议Astra中度执行Q-01，仅综合工程、旧档、完整生涯与确定性验证，不新增产品功能。原因是需要分清旧兼容流程与当前报告入口，以及新旧数据语义；执行边界仍为证据/测试。Q-02另做三专项组合实页、375视口补项与退役导出，Q-03整理交付，Q-04交用户最终验收。当前不启动这些工作。

已读现有`gameStore.fullCareerAudit.test.ts`：36个固定种子/策略组合、六组确定性重放及36局汇总；职业报告仍走`advanceAfterReport`兼容入口，训练策略仍传旧focus。它适合保留兼容基线，不能单独证明当前`advanceProfessionalReport`与新维护选项的完整生涯。Q-01要补有限的当前入口路径，不能把旧36局通过包装为新入口全覆盖。

## 4. ASTRA-Q01完整指令（待用户转交）

```text
你执行《上场》CEU-20260907的ASTRA-Q01，使用GPT-6 Astra中度，由Codex统筹验收。
项目：/Users/zhihu/Documents/足球生涯游戏。
本指令经用户转交后仅授权Q-01：综合工程检查、旧存档矩阵、完整生涯及确定性验证。T/M/F专项均已验收，F04-V01关闭；其他已关闭问题保持。完成后待验收即停，不进入Q-02/Q-03/Q-04/R，不提交、推送、部署，不创建代理。

一、上下文和保护
读取适用AGENTS.md、docs20第5/7/10节、docs21当前结论、docs23/25批准规则与docs39；按需要读取T02迁移、M02-R1/M03保存、F02/F03路由和F04-R1证据索引。
核对HEAD预期3f290407a299790665b58c4bc7c2bf8455d24bbd、SAVE/DATA12/12、git status，保存本批前工作树/源码/冻结证据与outputs哈希。已有未提交改动全部保留，不reset或覆盖。
新证据独占docs/evidence/CEU-20260907/Q-01/；所有已有证据只读，旧采集开关继续拒写。不访问用户真实浏览器/生涯，不修改生产代码、CSS、参数、迁移、版本、依赖或球队数据。
可新增必要的验收测试/脚本及文档，优先独立新文件，不改写已冻结基线断言。开始时统一Q-01行和第10节为进行中；先写case-plan与命令/样本上限。

二、全量工程与现有36局
运行npm run test:run、npm run typecheck、npm run build并保存命令、时间、退出码、完整日志。已有全量测试含gameStore.fullCareerAudit.test.ts：36固定组合、6确定性重放及汇总，提取本次FULL_CAREER_AUDIT_JSON，不把这些重复执行计为不同种子。
保留原36组合、策略、种子、日期和原断言；查看与升级前可核验同来源汇总的变化，区分新训练/市场批准规则造成的变化与未解释差异。找不到可比原汇总就标缺口，不自行生成伪旧基线。不以36局小样本断言最终平衡。
确认公开store路径到退休，无阶段死循环、无非法合同/队伍状态、年龄终点合法及数据有限。已有脚本允许1000动作上限，不放大上限掩盖卡住。

三、补当前入口的有限完整生涯
现有36局仍走advanceAfterReport与旧focus，仅证明兼容路径。独立新增3条固定、可复跑的公开store完整生涯：从新建到合法强制终点，使用当前职业报告advanceProfessionalReport及正确windowIndex/careerSeed；必要合同/转会/退役确认不跳过，少年流程按其本来入口。
选定3个种子/策略后不循环寻找漂亮结果；每局上限1000动作，记录决策策略和每一步窗口/phase/history长度、关键保存。按真实可选训练执行，31/34边界前后及维护说明/规范化焦点有记录；三种维护焦点用预定策略分配，不以旧attack自动规范化冒充主动选择维护。
在合法成年/老将阶段通过公开方法更改当前求职方向，证明开局draft/旧history不追改，已生成报价及谈判不变，下次实际生成市场使用新方向。记录实际是否留洋/回国，不保证指定种子必达某球队，不构造成功履历。
若三个新生涯未自然覆盖35岁海外组合，单列缺口并交Q-02选用既有真实/明确构造档，不能额外无界搜种子。维护自动恢复等已有精准边界测试可引用核验，无需每条长生涯强造低状态。
这3条各重复一次，比较完整最终game和关键步骤/保存记录；固定初始时间/种子，明确是否有需排除的非游戏元数据及原因。未结算动作不得重复增加history或收入；合法到队成本单独记录，不能错误断言整个生涯现金不变。正常结算每次只新增一个半年，旧history前缀保持。
通过store调用验证不等于真实UI验收，组合实页留Q-02。

四、统一旧档矩阵
先索引既有T02的21输入、M系列15原始市场档及14旧phase组，以及F系列补充状态，按原raw SHA去重。每项写清自然/构造/操作中间状态、原版本、phase、来源和既有断言；去重不能丢失同raw的不同合法分支覆盖，不简单把各批数字相加当独特样本数。
覆盖旧训练计划、事件处理中/结果、报告、旧结束页/退役决定、已有报价和谈判、到期外部0–3及续约、临近退休。优先复用既有有限恢复驱动/断言，不扩所有维度笛卡尔积。
原raw首次直接走现有加载入口（不先重存成v12，不借backup掩盖损坏），随后执行对应合法公开动作→保存→清空内存重载，记录实际Storage、game、允许迁移差异和保留字段。v11旧历史/旧报告不追改，规范化只发生在批准的待执行焦点；已有报价不重抽、谈判不复位，零外部续约可继续。
已有测试若已完整完成这条链并在本次全量中运行，可引用其本次日志并关联精确输入；若仅测load/helper或缺写盘证据，再补最小链路。缺口写清真实与构造，不能改原raw来使它通过。
这批不测试旧客户端读v12，不做降级回写/上线回退；交R另行决定。

五、失败处理、收口与交付
发现反例先保留最小输入、实际步骤/保存结果、错误与影响；区分主机失败、验收脚本假设和生产缺陷。脚本假设错误可在新验收文件最小修正并留失败记录，不删除断言掩盖问题。生产缺陷不自行修复，完成不依赖该缺陷的检查后交统筹；同源失败不反复重跑，不抬上限。
新增正式测试后需完成最终一次全量、typecheck、build；若首轮已是最终内容且全部通过，无须重复。保持旧产物保护，不重跑与本次无关的目录/二维码生成或真实浏览器遍历。
交付Q-01/README、case-plan、旧档去重/分支矩阵、36局汇总、3条新完整生涯与确定性证据、命令日志、实际修改清单/哈希，以及Q-02交接。Q-02交接注明三专项组合实页、375×667缺项、退役档案/图片/二维码核验及真实设备限制，不执行这些任务。
更新docs20/21和第10节：Q-01待验收（若有阻塞明确标注），T/M/F已验收保持；列已完成/复用/未执行及问题，不自行宣布全升级通过。
最终报告实际测试数、独特种子/重复次数、原raw去重数与分支数、终点/确定性结论、保护、HEAD/12/12、限制。若起过服务/临时上下文则关闭。
唯一下一动作：用户转交Codex审核ASTRA-Q01。完成即停。
```
