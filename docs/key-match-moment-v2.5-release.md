# V2.5.0：关键比赛时刻

当前为最终发布候选，尚未提交推送。游戏版本2.5.0，存档/数据版本13/13。

职业半年在符合实际一线队出场资格时可能出现一次关键时刻；首秀必触发。五位置组各8个模板，共40个，每次三选一，先保存结果，再完成半年。能力、状态与风险影响选择取舍；不模拟完整逐场赛程，不重算比赛胜负或冠军。界面的“当时”分钟/比分表示机会发生时背景，不会随着进球结果改写。

最近四次实际时刻优先不重复，跨空窗口保留记忆；合法池不足时从最旧限制依次解除，不借用其他位置。已生成选择、结果和报告保留原快照；刷新不重新抽取。

此前各阶段已通过统筹验收：40目录、1800行选择矩阵、100条合成轨迹、同100开局的300完整运行及9复演。CDM轮换组进球+10.71%的观察线已由统筹结合低基数/直接贡献接受，不据此宣称统计显著或最终全域平衡。默认测试包含独立列明的48份输入；大型生涯与图片证据保留本地，不作为npm默认测试的隐含依赖。

代码回退基线为`7421b442983d27bc17f535cf741a7f065b1ee87e`（V2.0）。V13写入前保留独立V12原始备份；旧客户端不能无损读取V13。回退时先保留当前V13原档，再明确选择使用旧V12备份，会丢失升级后进度；不能自动覆盖用户当前档。

最终发布仍须统筹核对干净候选，之后按已获用户授权提交main并确认Pages自动部署。真实手机、其他浏览器及线上本候选未验证；字体沿用紧凑基线，允许正常滚动，大包警告仍保留。

本地详细验收入口：`docs/evidence/KMM-20260913/P1-R/README.md`；大证据不随发布打包。正式部署后另记远端commit、CI、线上资源身份及实际体验范围，不能把候选准备写成已经发布。
