# 四联赛原创队徽替换指令与检查点

2026-09-12，任务 CLR4-20260912。用户授权 Codex 直接指挥既有任务「开发员 Astra 轻」完成英超、西甲、德甲、法甲队徽替换。Codex 负责内部审核，无需逐枚请用户确认。

## 当前基线与范围

工作区 `/Users/zhihu/Documents/足球生涯游戏`。本地 HEAD `6ca977e` 已合并重复冠军修复；已部署产品为 `2308687146df5d26c5f97965d3e8a1e998e9cb66`。既有R2记录已随6ca977e推送；本轮76枚仍未提交，不能盲目纳入推送。本轮完成接入与验收，发布作为单独检查点；不要把“完成替换”写成已上线。

先读本文和文档46，更新46中已过时的授权、基线及范围，保留意甲已完成历史。本文对本轮范围优先：英超20、西甲20、德甲18、法甲18，共76，不机械套用旧文档每联赛20枚的数量。以游戏目录而非现实联赛名单为准。

来源均在 `/Users/zhihu/Downloads/`：`英超队徽`、`西甲队徽`、`德甲队徽`、`法甲队徽`。统筹初查76枚XML可解析，未以此代替技术与视觉验收。西甲 `esp1-athletic-club (1).svg` 必须记录源名与目标名，明确对应Athletic Club，不修改下载原件。英超曾冻结的旧输入只能做比对，本轮重新按当前交付冻结。

允许改目标SVG、`src/data/clubs/clubCrests.ts`、直接相关测试和本轮文档。保护意甲、中国及其他非目标素材、俱乐部ID/名称/主题、荣誉修复、求职方向界面、游戏规则、版本与真实存档。默认不改共用CSS/组件/导出逻辑，不重绘或随意压缩设计师成品。必要非视觉技术适配先向统筹说明证据。

## 执行步骤和验收门槛

| 阶段 | 工作 | 完成标准 | 当前状态 |
| --- | --- | --- | --- |
| A | 冻结76源文件、旧目标及工作区；映射与SVG技术检查 | 每枚源/目标名、canonical/兼容ID、SHA、体积、外部依赖、字体、脚本、位图/滤镜与风险清楚 | 已完成（76原件/2414工作区文件冻结；技术检查通过） |
| ENG | 英超样板与全量 | 4枚代表样板→20全量；逐枚尺寸、实页、真实PNG通过 | 全量完成（统筹已放行样板；20枚/三视口/两PNG） |
| ESP | 西甲样板与全量 | 4枚代表样板→20全量；命名特殊项与映射通过 | 全量完成（样板统筹通过；20枚/三视口/两PNG） |
| GER | 德甲样板与全量 | 4枚代表样板→18全量；大体积/复杂SVG代表通过 | 全量完成（样板统筹通过；18枚/三视口/两PNG） |
| FRA | 法甲样板与全量 | 4枚代表样板→18全量；逐枚检查通过 | 全量完成（样板统筹通过；18枚/三视口/两PNG） |
| Q | 四联赛统筹收口 | 76资源与映射、工程/实页/导出、非目标保护、剩余问题准确记录 | 已完成（Codex独立复核通过；本地未发布） |

按 ENG→ESP→GER→FRA 串行接入，过程中不丢失后续阶段。每联赛先选4枚覆盖大小/细节/新接入的样板，向统筹发送样板图和简短检查结果；等待内部视觉放行时可继续其他只读检查。放行后完成该联赛全量，再进入下一联赛。安全或确实无法可靠渲染的单项明确标为阻塞，不把回退短标算作队徽成功。

全部76枚需核验24/32/48/96px及原比例，使用适量分组图并逐枚放大目视，逐枚结论落表。SVG需检查外部图片/字体、脚本事件、嵌入位图、mask/filter及浏览器导出支持。不得引入运行时下载目录依赖。可在项目复制时去掉源文件重复下载后缀，但必须记录映射且确认唯一性。

每联赛实际页面检查生涯/球员总览、市场报价、履历、退休；桌面1280×720、手机390×844，320×568补查易挤压页面。确认主题、布局和主要操作不因素材改变。用隔离匿名样本，不读写用户日常浏览器存档。每联赛至少桌面/移动入口各一份真实按钮生成的退休PNG，并确认新队徽不是短标、空白、裁切。可构造多队履历覆盖全部目标，严禁测试页面进入生产包。意甲、中国及未知ID回退用代表样本抽查并配合非目标SHA保护。

样板仅相关测试/typecheck；各联赛检查实际集合映射与文件存在。四批只增资源且规则未变时，最终精确候选统一运行全量/typecheck/build，不为每枚或每批机械重复全量。保留有意义的集合与兼容ID回归，不能只改总数量凑绿灯。已有测试证据与本轮新验证分清。

## 证据、协作与交付

使用独立 `docs/evidence/CLR4-20260912/` 保存简明源映射/状态/索引，原始截图、原件冻结与临时脚本放独立 `/tmp/CLR4-20260912/` 或项目证据子目录，不能覆盖旧CLR/CEU。主台账46和本表按阶段更新。记录可复跑路径、逐枚状态、原件/接入件差异及非目标保护；不制造重复日志。

完成后向统筹交：76枚最终映射与来源哈希、精确修改清单、各联赛样板/全量实际截图及PNG、工程结果、旧ID/非目标保护、未测范围、Git状态。真实手机/微信/Safari/相册未测应如实写；不要用模拟视口冒称真机通过。

本指令先收口四联赛替换与验收，不执行推送。已有用户持续授权在明确发布指令后可执行，不需要重问常规许可；发布需统筹给精确候选指令、核对本地未推文档与远端关系、保留回退点并检查Pages/线上资源。任何安全审批拒绝均明确报告，不间接绕过。

## 2026-09-12执行记录

A已冻结76份原件，共2,770,004 bytes；76个canonical/workbook映射与项目一级联赛精确匹配。没有旧目标SVG，已有清单原文已冻结。原件无脚本/事件属性/外部引用/字体/位图/filter/mask；XML与尺寸检查通过不代替视觉。英超先接入阿森纳、纽卡斯尔、埃弗顿、利兹联。源映射见[evidence/CLR4-20260912/source-map.json](evidence/CLR4-20260912/source-map.json)。

期间用户另直接授权推送既有提交，远端main已核验为6ca977e，仅补历史R2记录与合并关系，生产树仍与2308687相同。原文“未推R2记录”为指令写作时点，本轮CLR4仍不推送。

## 最终交付检查点

ENG20/ESP20/GER18/FRA18已完成执行；四轮样板均获Codex放行。Q待统筹最终验收，唯一下一动作是Codex审核[最终交付](evidence/CLR4-20260912/README.md)。76原件逐字节接入、逐枚尺寸/实际PNG目视完成；最终73文件735项、typecheck、build通过，890.48kB警告保留。本轮无提交/推送/部署，不宣称线上替换完成。
