# M-02补修验收与M-03交接

2026-09-08，Codex统筹。执行者原交付与冻结反例保持原样，本文件记录后续验收结果。

## 1. 结论

**ASTRA-M02-R1通过，MR-05关闭，M-02标为已完成。M-03未开始，待用户转交指令。** 该结论只覆盖资格、组合、抽取及到期稀缺市场持久化；整个M专项尚未完成，偏好编辑与MR-03防重抽仍待开发。

接受的补修：续约1份＋外部0–3份保存/恢复完整保留；仍检查现队续约、外部类型/排除现队、报价唯一性及有效选择。撤回历史可以保留，但不能把撤回报价当作当前合法选择。原年龄与损坏档处理不变，未新增字段或迁移，版本12/12保持。

## 2. 独立核验

- 生产差异审查：唯一生产改动是save.ts的validExpiryMarket局部，transfers.ts、gameStore.ts、训练及页面未改；符合文档31授权范围。
- 独立定向9文件133项通过，typecheck通过。其中包含4种报价数量、7条续约/外部签约后继、11项异常保护、8份原始市场旧档及训练迁移/年龄回归。
- 另以此前统筹冻结的4组完整beforeReload输入复验，未重新挑选样本：同一公开选择动作写盘后，续约＋0/1/2外部已完整保留；3外部对照保持一致。原始写盘envelope与内存全字段相等，无backup，清空store再loadGame/continueCareer仍相等。原反例中的清空结果未被覆盖。
- 独立核对19项R1产物SHA/字节、624项原有文件，617项未变；7项变化为6项声明编辑及1项tsbuildinfo缓存，新增R1测试after哈希一致。433份原冻结证据/outputs均不变。HEAD仍为3f290407a299790665b58c4bc7c2bf8455d24bbd。
- 公开谈判有限18次覆盖11成功/7失败；撤回只接受为明确构造的持久化补证，不扩大为本稀缺场景自然谈判覆盖。无需再搜种子。
- 全量64文件481项、build通过采用已核验哈希的执行者日志，本轮未重复执行。大包警告保留；本轮没有浏览器/模拟视口/真实手机/线上/旧客户端读v12验证。

复验源码和原始日志见[独立证据](evidence/CEU-20260907/M-02-R1-review/README.md)。本轮仅增加复核证据及协调文档，未修改生产、未提交推送部署。

## 3. M-03边界与实现前必须说清的状态

下一批为当前求职方向编辑、保存、生效提示及防重抽。沿用已确认D-M1/D-M3；不新增职业优先级或市场刷新机制。编辑只影响尚未生成的市场，当前完整报价及谈判不变。

现有reviewReport与goToPhase会直接持久化phase；openTransferWindow的到期分支先生成、空数组被误判为未生成，普通已开市场还可能先被机会节奏拒绝。这些入口和报告的必要只读显示属于M-03最小修复范围。F的报告主按钮合并、职业结束页简化、国家队/退役流程改造仍不进入本批。

实现前先列短状态表，至少区分：当前尚未开市场的已结算窗口、正在处理的非空/空市场、到期稀缺市场、已完成签约/到队、下一训练计划、旧回看/结束页持久化上下文。结合phase、windowIndex、最后history窗口、transferDecision及合同说明证据，不能仅用数组非空认定“可复用”。

恢复既有市场不是新开市场；应先识别合法已生成上下文并复用，再对真正新生成应用原机会/失约/年龄条件。不能因此允许任意阶段开市场或把上一窗口已完成报价重新激活。若发现合法旧档无法可靠区分已生成/未生成，提供相同关键字段却需不同处理的具体反例，交统筹处理D-C1，不猜造marketId或用时间补洞。

## 4. ASTRA-M03完整指令（待用户转交）

配置沿用GPT-6 Astra中度。当前未发送或启动下一批。

```text
你执行《上场》CEU-20260907的M-03，使用GPT-6 Astra中度，由Codex统筹验收。
项目目录：/Users/zhihu/Documents/足球生涯游戏。
本次用户转交授权仅M-03：当前求职方向编辑、持久保存、生效提示，以及既有市场防重抽/可靠恢复。M-02与MR-05已由文档32验收；不进入M-04/F/Q/R，不提交、推送、部署，不创建代理。

一、先恢复上下文
读取适用AGENTS.md，docs/20第4/5/6.2/7/10节，docs/23第2节，docs/25确认记录，docs/31与32，docs/21，M-01事实交接、M-02及M-02-R1/README，S0流程证据和M-02/m03-reentry.json。
核对git status、HEAD和12/12版本。预期HEAD为3f290407a299790665b58c4bc7c2bf8455d24bbd；保护全部未提交训练/市场修改、冻结证据和outputs，保存本批前哈希。不要访问用户真实浏览器存档。
授权后统一主台账顶部、M-03行和第10节为进行中。M-02已完成、MR-05已关闭，不重开参数或候选生成规则。

二、改动范围
允许gameStore.ts相关动作/守卫、必要的共享纯状态判定、小型求职方向编辑器、TrainingPlanScreen/TransferWindowScreen/PlayerScreen，以及防止回看改phase所需的App/careerNavigation/HalfYearReportScreen等最小配套与局部样式。
不改transfers.ts的资格/组合/权重、训练公式、球队目录、依赖或职业优先级。默认不改存档结构、版本和迁移；如旧档不可区分需新增字段，先交具体证据，不自行添加。
报告只读模式和必要调用点适配属于本批；不要提前合并职业结束页、重排报告主动作或改退役/退出国家队流程。这些仍属于F。

三、当前求职方向
1. 新增updateCareerPreferences(intent, leagues)，不得复用submitPreferences。后者会重生球员和开局流程。
2. draft保留开局档案；只更新player.overseasIntent和player.preferredLeagues。player.priorities/priorityValues以及其他状态不变。Player页明确当前方向，开局信息从draft取，不追改历史。
3. 方向仅STRONG/CONDITIONAL/DOMESTIC，联赛来自现有合法目录、去重、最多3个，DOMESTIC保存为空列表。UI与store使用一致校验；无效动作不能部分写入或悄悄修改其他字段。
4. 只允许已有职业合同、非退役决定/已退役、非SIMULATION_READY或事件处理中的上下文编辑；先列phase允许表，与两个页面实际入口及公开store守卫一致。
5. TrainingPlanScreen与TransferWindowScreen复用一个非必填小编辑器，按钮“保存求职方向”。未保存修改仅本地表单，取消/刷新丢弃；保存后刷新保留；重复保存相同规范化值是无操作，不写新存档、不消耗随机、不生成报价。
6. 未生成市场提示“用于下一次生成的报价；不会立即开启市场”；已生成提示“本次报价和谈判保持不变，下次市场生效”。不要把编辑文案写成回国保证。
7. 保存时phase/windowIndex/seed/cashEuro/history/lastReport/pending事件/draft/报价/选择全部不变，反复修改只改变当前两个字段。尚未生成的下一市场沿原合法流程开启时，使用最后保存的方向。

四、防重抽及恢复
1. 先按文档32第3节写状态表，再实现共享判定，供入口守卫/提示复用。必须识别正常市场、空市场、到期续约＋0–3外部、已签约后的状态及旧回看上下文；不只以transferOffers.length判断。
2. 合法已生成市场恢复应先复用原报价/选择/谈判，不重新经过新市场机会筛选，也不在到期分支优先重生成。窗口不前进、不重结算。真正未生成才按原节奏、表现、失约、年龄、合同条件开市一次。
3. 回看报告使用只读视图，不持久化game.phase，不改窗口或报价；返回准确回当前进度，不能显示能再次结算/推进的按钮。切换球员/履历/设置、刷新及换生涯不得泄漏旧只读上下文。保护相关旧store动作，不能仅隐藏UI却留下直接调用重抽路径。
4. 必要守卫可覆盖reviewReport/goToPhase/advanceAfterReport/openTransferWindow等相关入口，但保持正常开局、青训报告和当前职业推进可用。过期动作或重复调用不再生成、扣款或推进，已签约/到队状态不能被当成未处理市场。
5. 原报价逐字段保留：clubId/id、薪资、角色、费用、counterUsed/counterDirection/negotiationSucceeded/negotiationMessage/withdrawn、顺序及selectedTransferChoiceId。不要为了新方向改写已有报价，也不要让已处理谈判重新可谈。
6. 保护MR-05：到期稀缺市场不能因编辑、回看或恢复变成空报价/旧结束页；续约和外部签约后继仍可用。真正下一计划清旧市场沿现有协议，不拿上一窗报价复用。

五、验证
1. 默认无任何历史采集开关。将MR-03四类旧缺陷断言替换为正确目标断言，列对应表，原M-01/M-02 JSON和源码副本保留。不得整文件skip或覆盖旧快照。
2. 验证三方向、联赛去重/上限/非法值/DOMESTIC清空、取消/未保存刷新、保存刷新、连续三次修改、重复同值无操作及禁止phase。存储前后全state差异只能是两个player字段；用生成/随机调用观察确认保存不抽市场。
3. 当前已生成报价在上述操作后完整一致；下一次真正未生成市场使用最新方向。使用固定输入对照验证生效，不靠一次碰巧出现国内报价证明。
4. 覆盖普通非空、普通空、到期续约＋外部0/1/2/3、谈判成功/失败/撤回、已选择外部、已签约/到队/下一计划。M-01原8档和R1稀缺输入优先复用，构造必须标来源和差异。原始envelope直读、独立Storage无backup，再公开合法后继，不以先重存data代替读取。
5. 对每类关键市场检查：回看往返、页面导航、保存加载、重复开市/过期推进动作，都不改报价、谈判、选择、现金、history、事件、报告或窗口。正常新市场仅生成一次、窗口推进一次；正常新半年仍可继续。
6. 尤其区分：恢复已生成市场不受新开机会节奏阻止；未生成市场依然受原条件限制。不要通过取消机会或年龄检查让两者都“成功”。
7. 运行相关store/persistence/navigation/view及市场、训练回归，随后全量test:run、typecheck、build，保存原始日志。真实页面在隔离浏览器/独立origin使用匿名测试档，验证1280×720及390×844的编辑、取消、保存、刷新、回看返回和重复点击；320/430宽度检查编辑器局部换行、按钮可达及不遮挡。截图要实际查看，不能只以无横向溢出判通过。真实手机未执行须保留。
8. 同时记录生成层、保存/加载、store动作和实际页面证据，不能互相替代。复用已验收训练页面结构，不扩大视觉改造。发生确定的存档/流程歧义先记录具体阻塞，不用脆弱兼容猜测放行。

六、交付并停止
新证据只写docs/evidence/CEU-20260907/M-03/，含状态表、动作/字段契约、旧断言替换表、真实页面截图与操作、原始命令日志、完整恢复证据、修改清单及前后保护哈希。新产物独占写入，日常测试只读。
同步确受影响的docs/01/02及docs/21市场栏；docs/05无参数变化无需改。更新docs/20顶部、M-03行和第10节为待验收；MR-03标修复待统筹验证，不自行关闭。M-04及F/Q/R保持未开始。
摘要报告实际测试数量、旧档/稀缺档结果、防重抽与下一市场生效结果、页面验证尺寸、HEAD及限制；保留大包警告、真实设备/线上未执行项。
完成后停止，唯一下一动作：用户转交Codex审核ASTRA-M03。不提交推送部署，不自行进入M-04或F。
```
