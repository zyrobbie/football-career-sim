# S0复审结论与训练专项交接

日期：2026-09-07；统筹：Codex；HEAD：`3f290407a299790665b58c4bc7c2bf8455d24bbd`。

## 1. 复审结论

**S0-03通过统筹技术审核；用户于2026-09-07确认第2节规则并授权启动T-01，S0-04已完成。本批仍不修改生产行为。**

文档23的ASTRA-S0-R1修订已补齐四项必要约定，本轮不再要求方案返工。这是方案可实施性审核通过，不是新规则、迁移或页面已经实现并验收通过。

| 意见 | 关闭依据 | 开发阶段仍须验证 |
| --- | --- | --- |
| SR-01 | 第1.5节复用eventSummary，合同、事件和国家队组装均保留该字符串，旧报告不补算 | 完整组装链、存档重载和页面可见性 |
| SR-02 | 第1.6节分开选择前预判、事件后权威判断和策略后状态；低状态不显示无效维护卡 | 45/46阈值、事件改变预判、饱和旧选项、恢复优先的实际文案 |
| SR-03 | 第4.2节明确冻结v11与v12逐份迁移测试分离，旧生成器已禁写；老将补采列为生产修改前门槛 | 老将夹具补采、独立加载继续、重新保存读取；不得覆盖原七份档 |
| SR-04 | 第4.1节规定canonicalState统一供引擎、保存及新history使用；已结算历史不改 | 重复加载幂等、事件不重做、计划与实际覆盖说明一致 |

本轮独立执行：`npm run test:run -- src/store/gameStore.ceuS0Baseline.test.ts`，退出0，1文件／4项通过，13:07:16开始、0.712秒。七份夹具SHA-256及字节数匹配，四对.log/.txt逐字节一致。typecheck及拒写验证核对了R1实际日志和生成器代码，本轮未重复运行。未执行全量、build、页面／手机、新迁移或新参数模拟。

## 2. 提交用户集中确认的规则

以下规则已于2026-09-07获用户确认，已归档主台账D-*和S0-04。确认原话：“好，给Astra 轻度发明确指令”。技术细节以修订文档23为准，维护参数仍属首轮候选值。

| 对应决定 | 推荐锁定内容 |
| --- | --- |
| D-T1／D-T2 | 30岁及以前保持原规则；31–33岁引入维护，34岁起三种维护。身体维护：身体状态最多+4、竞技−2、身体衰退减缓20%；比赛状态：竞技最多+4、身体−3；心理调适：心理状态最多+4、竞技−2。保持状态上限与年龄衰退；自动恢复优先。参数作为首轮实施候选，定向模拟后由统筹检验，不预称最终平衡通过 |
| D-M1／D-M2／D-M3 | 开局档案保留，职业期可改当前方向；综合考虑在候选充足时国内／海外各至少一份；优先国内至少两份国内；优先海外优先海外候选。外部最多三份、到期续约另计；新偏好只影响尚未生成的市场 |
| D-F1 | 普通报告一次进入下一计划，关键合同／去留／退役与国家队退出入口保留；旧结束页状态可恢复，不重复结算 |
| D-C1 | 新增三个训练枚举，采用明确11/11→12/12迁移；旧历史、报告、报价保留；未来窗口用新规则。旧客户端读回新存档不作保证，发布前落实回退／备份方案 |

确认本表意味着锁定上述规则并授权开始训练专项准备；不意味着批准提交、推送或发布。后续如测试发现重大平衡问题或需改变已锁定收益／成本，由统筹提出具体调整，不自行扩展系统。

## 3. 下一批：ASTRA-T01，轻度配置

**当前状态：S0-04已完成，用户已授权启动并发送；本批只做T-01。**

```text
你执行《上场》CEU-20260907的T-01，配置GPT-6 Astra轻度，Codex主任务统筹审核。

工作区：/Users/zhihu/Documents/足球生涯游戏。
先读docs/20台账第4/5/10节、docs/23第1及4节、docs/25全文，检查当前AGENTS.md、HEAD、git status。
只有主台账已记录S0-04通过和本批授权才启动；不要把本文件中的“推荐”自行改成用户批准。

范围：冻结训练变更前对照、补采v11老将存档，完成T-01；不改生产源码、数值、版本或页面，不做M/F。

1. 保护S0原七份档及原始证据，不恢复CEU_GENERATE旧写入开关。记录所有既有改动，使用匿名测试数据和内存Storage，不访问用户浏览器存档。
2. 在当前生产代码仍为锁定v11基线时补采文档23第4.2节老将状态：35岁未选计划、已选attack/physical的事件处理中及事件结果、SIMULATION_READY，以及低状态/饱和场景。各阶段实际可达性单列。
3. 使用公开store流程；短暂SIMULATION_READY可从测试内Storage钩子捕获真实保存值，不伪改报告phase。种子搜索限ceu-v11-supplement-0..99，找到覆盖即停止。无法公开到达的场景记录缺口，构造单元输入单独标识。
4. 新夹具只写docs/evidence/CEU-20260907/S0-v11-supplement/；生成器核对版本、生产源码基线及输出目录，使用独占写入防覆盖。每份写manifest、SHA-256、seed、实际选择序列和可重复命令，验证真实加载及后继动作。
5. 冻结13–30岁代表性输入与原引擎输出，供后续验证年轻阶段行为不变。按文档23第1.4节分开100种子代表组和年龄/状态边界组，不做巨大全组合。尚未实现的新维护效果只登记未来断言，不能伪造通过。
6. 给出T-02将直接使用的fixture/测试入口、规范化焦点及报告链验证清单。冻结历史复演与未来v12迁移用例明确分离。
7. 运行相关定向测试和typecheck；测试使用当前常量还是旧版本常量须区分。无生产改动无需完整浏览器或构建验收，不把未执行记成通过。

交付：新增测试/生成器、匿名夹具及证据索引、覆盖与缺口表、年轻基线结果、T-02交接摘要；更新主台账和第10节。
夹具未达成的缺口交统筹判断，不自行通过T-01。完成后停在统筹验收，不开始T-02，不提交、推送或部署，不创建并行代理。
```

## 4. 后续配置与推进

- T-01：Astra轻度，完成上述基线与补采后交统筹验收。
- T-02：Astra中度，负责维护逻辑、规范化焦点、11→12迁移及恢复；届时另发针对实际T-01交付的精确指令。
- T-03：Astra轻度，接口稳定后接入适龄页面及报告，不提前合并F流程。
- T-04：按完整训练验收收口，统筹审核证据与参数表现。

只在实际技术或产品决定需要时更新方案；常规实现和验证遵循已锁定范围，不逐文件重复确认。
