# T-03统筹验收与T-04交接

日期：2026-09-07；统筹：Codex；HEAD：`3f290407a299790665b58c4bc7c2bf8455d24bbd`；改动未提交。

## 1. 结论

**T-03通过，标为已完成。T-04待用户授权转交，未启动。**

分龄选项、默认值、低状态恢复入口、饱和新选禁用、旧计划继续及保存摘要呈现已达到本批要求；未发现需要阻塞下一阶段的实现问题。本结论只覆盖两种代表视口和已验证路径，不是全部设备、长期平衡或发布验收。

源码审核重点：选项集中在trainingPlanView，复用年龄与focus规范化；页面用生涯/窗口/已存计划key隔离草稿，草稿有16项容量上限；提交前读取当前store并校验phase/key，以ref防重复进入，错误时允许重试。页面不提前执行维护，不在effect中推进；报告仅显示已保存字符串。T-03对trainingPlan引擎文件的变化仅在摘要格式，参数、迁移与版本未变。

## 2. 独立工程与页面验证

| 层次 | 本轮结果 |
| --- | --- |
| 全量 | `npm run test:run`，60文件／415项通过，15:54:13开始、14.75秒，退出0；包含定向相关用例，未重复单跑定向 |
| 类型与构建 | typecheck、build退出0；167模块，主包851.92kB／gzip236.63kB；>500kB警告继续保留 |
| 哈希 | T-03的66项artifact-hashes全匹配；before-hashes中299份声明不变文件逐份匹配，7份已有文件为授权修改；冻结数据和outputs不变 |
| 实页环境 | Browser插件未提供，按前端测试技能使用已有Playwright与全新Chromium上下文；未安装依赖，未访问用户profile |
| 启动记录 | 原4183端口已占用，新启动Vite使用127.0.0.1:4184；复跑脚本只改临时输出目录与本地端口，不覆盖执行者证据 |
| 主机限制 | 首次沙箱内Chromium启动因MachPort Permission denied失败；随后经工具审批在沙箱外启动隔离上下文成功，不能把第一次失败记成产品错误或通过 |
| 页面流程 | 冻结匿名档/明确构造的年龄状态→页面选计划→返回保留/新窗默认/恢复与旧选提交；另以真实旧envelope执行load→训练→事件→新报告→保存重载 |

| 实页检查 | 结果与证据 |
| --- | --- |
| 页面身份 | 本地4184，页面标题与项目相符；复跑browser-results.json记录URL/title |
| 非空/错误覆盖层 | 训练卡、报告正常呈现；截图与目标元素断言未见空白页或框架错误层 |
| 控制台 | 捕获pageerror、console error/warning，最终均为空 |
| 交互 | 心理调适选择在重渲染及“球员→生涯”返回后保留；同DOM两次点击只记录一次；新窗默认身体维护；恢复提交BODY_CARE/STEADY；旧饱和attack提交MATCH_SHARPNESS/STEADY |
| 真实结算 | 清除spy后从原生v11 PLAN_35经真实store结算并保存v12报告，重载摘要一致；动作次数spy不替代此证据 |
| 视觉 | 1280×720和390×844，复跑52张截图，与执行者原图逐字节一致；人工抽查年轻/31岁/35岁/恢复/饱和/旧计划/新旧报告，说明可读、禁用原因可见，正常滚动后CTA未被底栏挡住 |

完整元素截图只用于读完整内容，不用其中固定底栏的位置判断可点击性。CTA判断结合submit视口截图和实际click。移动长页允许滚动，桌面原有左侧空白和移动其他统计区小字号未作为本批重做项目；后续综合验收仍记录整体阅读负担。

原交付与长期保存截图见[T-03索引](evidence/CEU-20260907/T-03/README.md)。统筹独立复跑临时脚本 `/tmp/ceu-t03-coordinator-qa.cjs`、结果 `/tmp/ceu-t03-coordinator/browser-results.json`、截图 `/tmp/ceu-t03-coordinator/screenshots/`；临时目录不是唯一证据，持久复跑方法使用原脚本副本，输出到新的临时目录。不要覆盖原52张截图。

## 3. 未完成事项

- 真实手机、Safari/Firefox、更多视口未验；不把390×844模拟视口称作真实手机。
- T-04需要跨年龄、跨多个窗口的综合训练验收，确认页面承诺与实际变化能对应；900次单窗配对不能证明全生涯长期平衡。
- 内部滚动备份不等于持久v11回退保障；发布前仍需落实备份与恢复流程，当前不提交或发布。
- 本轮只改主台账和新增审核文档，未修改生产源码；原证据中历史“待验收”状态保留，由本文记录后续结论。

## 4. 下一批完整指令（供用户转交，待授权）

建议配置：GPT-6 Astra轻度。若发现迁移/状态问题，先提供复现交统筹，后续再分派修复。

```text
你执行《上场》CEU-20260907的ASTRA-T04，配置GPT-6 Astra轻度，Codex统筹验收。
工作区：/Users/zhihu/Documents/足球生涯游戏。
只有用户明确转交本指令授权T-04后开始；本文件存在本身不是授权。只做训练专项T-04验收与文档收口，不进入M/F/Q，不提交推送部署，不创建代理。

先读适用AGENTS.md、docs/20第4/5/6.1/7/10节、docs/23第1/4节、docs/27和28，以及T-01/02/03证据索引。核验HEAD、git status、本批前哈希，保护冻结证据、已有改动与outputs。预期HEAD为3f290407a299790665b58c4bc7c2bf8455d24bbd，本地版本12/12，生产改动未提交。记录本次授权，主台账顶部/T-04行/第10节同步进行中。

本批以验证为主，不自行改参数、引擎、迁移或全站布局。发现问题时给出最小复现、影响、期望/实际和修复建议，交统筹决定；不能把失败用改快照或放宽阈值消除。

1. 新建docs/21-career-experience-upgrade-qa.md作为验收总表，以T-A逐条映射现有证据、此次补证、结论及剩余风险。先复用仍有效证据，只补缺口；M/F/Q/R保持未开始，不扩大为整项目发布验收。
2. 旧档综合恢复：直接读取原S0七份、真实老将十份与TR-01四份构造档，使用隔离内存/浏览器测试存储；验证未选、事件中/已选路线、事件结果、READY、报告/结束页/市场/临退各合法后继。旧history、事件与报告不改写，规范化计划与新增history一致；至少覆盖新页面参与的旧选继续、低状态恢复及饱和旧选。重复点击、保存重载不重复结算。明确17份真实与4份构造，绝不声称全部真实采集。
3. 跨窗口：在匿名固定输入中覆盖30→31、33→34、36→37，记录起始快照SHA/种子/选择序列。通过公开store推进，不手改phase伪造完成；观察选项/默认更新、旧局部草稿不会带入新窗、每窗恰好一次结算、下一报告实际计划与状态变化一致。若为构造年龄输入，明确标注，不冒充自然生涯。
4. 有界长期观察：最多3个固定种子，先写明种子及选择政策，再对每个相同35岁起始输入分支比较三项维护，沿公开流程推进至可退役终点或最多10个窗口；遇饱和/恢复按既定UI允许路径选择并记录实际focus，不强行绕过禁用。可用相同非饱和维护政策作为观察对象，但不得把实际选择发生分流的后续结果称为严格单变量对照。记录状态范围、累计能力衰退、恢复/饱和次数、实际训练解释及是否有流程卡住。该小样本只检查持续可用性和明显失衡，不承诺统计显著、胜率改善或完成最终全生涯平衡证明；不扩大无界搜索，不擅自调参数。
5. 页面补证：复用T-03的1280×720、390×844；对受影响训练与报告增加1366×768、320×568、375×667、430×932。选取31岁六项、35岁三项/饱和旧选、恢复、长报告等代表场景，不做所有状态×尺寸巨大全组合。隔离Chromium使用实际页面操作，检查层级、换行、遮挡、可滚动性及CTA可点击；记录控制台。模拟视口与真实手机分开，无法验证真实手机时记录未执行，留Q-04决定是否接受限制，不自行假装设备通过。
6. 确认报告只显示保存值，新摘要可理解、旧摘要保留；没有内部枚举或隐藏潜力泄露。单窗直接收益/成本与整窗净变化分别解释，不强求比赛结果改善。工程上运行必要定向、全量、typecheck、build一次；后续仅因实际失败或改动复跑。不覆盖冻结产物或开启历史生成开关。
7. 同步docs/01/02/05中与当前训练实现有关的内容；总表明确工程、存档、页面、模拟视口、真实设备、长期观察、发布回退各层状态。记录大包警告，v11读回v12及持久旧档备份/回退仍为发布前未完成项，不在此批准发布。

交付：docs/evidence/CEU-20260907/T-04/索引、场景与逐步操作、匿名新结果、命令日志、截图、哈希及未完成项；docs/21总表；主台账顶部/T-04行/第10节同步待验收。汇报已证实结论、失败/限制、HEAD、修改范围，停止由用户转Codex审核。没有统筹验收，不把训练专项或T-04自行标完成，不进入M-01。
```

## 5. 检查点

T-01、T-02、T-03已完成；当前唯一下一步是用户授权转交T-04。本轮未自动发送指令或启动下一阶段。
