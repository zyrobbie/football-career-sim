# V2.0 半年计划布局升级：执行指令与台账

2026-09-13，TLD2-20260913。用户授权Codex出指令并验收，指挥「开发员 Astra 轻」完成升级并推送部署；完整交付版本V2.0/package2.0.0。执行模型GPT-6 Astra轻度。统筹持有直接发布授权，不重复询问普通许可。

## 基线与诊断

目录 `/Users/zhihu/Documents/足球生涯游戏`，main基线 `034324dd4fdbc0437cc9b7132b8846988eb4854d`，package1.9.0、SAVE/DATA12/12。实时核对身份并冻结既有跟踪文件及旧证据，保护未跟踪资料/outputs，不git add .、不清理/reset。

用户截图Downloads/IMG_3323.PNG、IMG_3325.PNG、IMG_3326.PNG只读。统筹诊断 `/tmp/TRAINING-DENSITY-20260913/`：402px宽30岁策略115px/训练121px，31岁421px/506px；临时移除年龄类恢复115/121。trainingPlanView以31岁启用veteran；main.css8491附近T-03覆盖将选项改单列、字号升至14—18px并展开全部说明；恢复另有大卡。87d4e0e只修求职编辑器，未覆盖此处。历史验收只约束可读/可滚动/可点，漏掉跨年龄密度。

## 实现要求

1. **共享布局。** 年龄仅决定选项及内容，不决定字号、列数、padding。整理替换现有training-plan--veteran与训练/策略规则，不再末尾叠加覆写。手机策略3项一排，训练六项两排、三项一排；窄屏自然换行。桌面沿既有页面结构，消除年龄造成的纵向差异，不扩展全站改版。
2. **信息分层。** 选择按钮显示名称与简短收益/代价。每项维护的状态收益及成本在选择前可见；三策略短利弊可比较。选中项下方统一紧凑说明区提供完整含义/边界；通用规则可用原生details查阅。禁用原因、零收益仍扣成本、恢复优先与旧计划继续提示不能只藏在折叠区。不能简单删年龄类让旧CSS隐藏重要说明。标题建议12—13px、短说明11—12px，与邻区协调，不照搬旧8px正文；具体以真实页面调整。
3. **恢复提示。** 合并重复标题与导语，保留恢复原因、暂不选策略、事件后最终状态决定执行的含义；紧凑提示区取代大卡。成长提示无需重复堆叠，必要内容可查；保存旧计划/饱和/禁用及提交语义不改。
4. **范围。** 主要允许TrainingPlanScreen.tsx、trainingPlanView.ts或一个必要展示helper、main.css相关块及直接相关测试。engine/store/迁移/年龄阈值/随机/数值/数据/队徽/求职编辑器/报告/导出不得改。展示helper不能改变trainingSubmission语义；相邻.training-event-summary报告样式不可误删。不加依赖。
5. **版本。** package.json、package-lock根及根package版本统一2.0.0，真实展示同源，核对是否有硬编码1.9；不改依赖包版本。SAVE/DATA仍12/12，无需迁移；设置实页看到2.0.0/12/12。

## 验收标准

- 30/31岁相同视口的策略列数/字号/间距必须一致。覆盖31/33/34/37、45/46恢复边界、95/100饱和和旧饱和选择。30→31、33→34至少公开流程各推进一次，构造起点明确；保存恢复保持选择、不重复结算。
- 320×568、390×844、402×874、430×932、1280×720。主402/390检查年轻、31六项、34三项、恢复、饱和；其他视口抽查最长说明与三项分支。不盲做全组合，每类有实页。
- 390/402正常31岁：策略选择块（标题+3按钮，不含共享详情）目标≤170px；六项训练网格≤210px；恢复区≤160px；完整默认计划区目标≤750px。320允许合理增高但不能回到整列大卡。不能靠隐藏关键说明或极小字体达标，超限给统筹具体测量/截图，不以“能滚动”自行放行。
- 真实点击三策略/训练、展开/收起说明、恢复提交、禁用与旧选继续、保存重载、开始→事件→报告→下一计划。CTA与详情正常滚动后可点击，不用元素截图里的固定底栏位置证明遮挡或可达。
- 前后截图实际目视：比例、选中/禁用、收益代价/原因、换行、CTA，邻接求职方向/履历保持。首个真实视口和滚动后动作分别截图；DOM无溢出与绿测试不能替代视觉结论。Browser plugin未提供时按frontend-testing-debugging skill使用已有隔离Playwright，新context，不接触用户profile/存档。
- 定向测试保证信息映射和选择行为，不只断言CSS字符串。最终一次全量test:run、typecheck、build。保护范围外文件SHA、2.0.0/12/12身份；保留大包警告，不重构打包。

## 分工与发布

Astra立即执行A/I/Q/V，完成交精确候选路径/SHA、工程结果、逐场景高度/实际图、完整说明映射与限制，停在统筹验收，不提前Git提交或推送。文档与紧凑证据放 `docs/evidence/TLD2-20260913/`，本台账同步；截图/raw/脚本/日志放 `/tmp/TLD2-20260913/`，旧证据不覆盖。不要把改版本号当完整交付。正常final，统筹共享文件/wait读取，不创建代理。

统筹独立检查源码、跨年龄密度、恢复/边界，必要时明确返修。Q通过后用户授权正常精确提交main，保留并推送 `rollback/tld2-v2-20260913-before` 到基线；GitHub zyrobbie/football-career-sim的main自动Pages部署，站点 https://footballcareer.zyrobbie.site/ 。执行者转述授权若被自动审批阻塞，统筹凭直接用户授权正常审批执行，不换途径绕过。检查产品CI、线上JS/CSS、设置2.0.0及代表31/34/恢复页；最终文档收口，临时Git索引验证回退保留新文档。原始证据归档outputs/TLD2-20260913本地保留不进网站。关闭自己的服务及浏览器。

## 台账

| 阶段 | 状态 | 下一动作 |
|---|---|---|
| A 基线/诊断 | 已完成 | 实时保护与20组同输入前图 |
| I 布局与信息分层 | 已完成 | 共享三列、选中说明与紧凑恢复区 |
| Q 功能/视觉 | 统筹已验收 | 20组布局、4条提交链、747全量；见[交付索引](evidence/TLD2-20260913/README.md) |
| V 版本2.0.0 | 已完成 | 设置实页2.0.0/12/12，依赖未改 |
| R 发布/回退 | 已上线并核验 | 产品21d9814，Pages34705833319成功；回退标签已推送 |

真机微信/Safari/相册未实测不得声称已测。本次不扩展报告或导出验证。本批已完成；无待实施任务。

## 发布完成记录

产品 `21d98144b0479a1198be644e86fa048f134b88b9` 已推送到用户确认的公开仓库zyrobbie/football-career-sim/main；[Pages34705833319](https://github.com/zyrobbie/football-career-sim/actions/runs/34705833319)测试、构建和部署全部成功。自动审批曾要求具体公开目的地确认，用户明确确认后，同一正常推送成功；没有更换途径。

线上HTML、2个JS和CSS共4资源HTTP200且SHA匹配已验收构建。402×874隔离Chromium实测31普通/34维护/32恢复三条链，共34次真实点击、3个新半年，每条恰好+1履历后回合法计划；实际设置显示2.0.0/12/12。31岁策略119px、训练141px，恢复81.8px，与本地验收一致。原始构造输入、截图、动作和SHA见[统筹验收及发布](evidence/TLD2-20260913/coordinator-acceptance.json)。

回退标签 `rollback/tld2-v2-20260913-before` 已推送，指向 `034324dd4fdbc0437cc9b7132b8846988eb4854d`。临时Git索引验证对产品6个代码/测试/版本路径逆向应用变更，可恢复基线且保留新文档；不对真实工作区reset。必要时在独立分支准备该6路径逆向补丁并正常提交/push。

完整原始证据在本地outputs/TLD2-20260913，不发布到网站；服务/浏览器已关闭。纯文档收口提交标记skip ci，产品树不变，线上仍是上述已验证产品。真机微信/Safari未测试，大包警告911.56kB保留。
