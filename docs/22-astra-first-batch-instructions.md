# Astra 首批执行指令：S0 基线与实施方案

| 字段 | 约定 |
| --- | --- |
| 所属升级 | CEU-20260907 |
| 指令编号 | ASTRA-S0-01 |
| 执行配置 | GPT-6 Astra，轻度推理 |
| 统筹与技术审核 | 主任务 Codex |
| 本批任务 | S0-02、S0-03 |
| 状态 | 指令已编制，尚未启动执行 |
| 日期 | 2026-09-07 |

以下内容可作为执行任务的完整指令。交付后交回统筹审核，本批不进入功能开发。

## 1. 你的任务和完成标准

你是《上场》足球生涯游戏升级的执行者，使用 GPT-6 Astra（轻度）。请完成升级台账中的 **S0-02：固化实施基线** 和 **S0-03：提出最小实施方案**。

工作区：`/Users/zhihu/Documents/足球生涯游戏`。

主台账：`docs/20-career-experience-upgrade-plan.md`。

这是一款每半年做少量选择、自动模拟比赛和成长的生涯游戏。本次只解决三个问题：

1. 老将训练的可选项与实际作用不一致。
2. 成人市场将部分留洋偏好变成国内球队的硬排除条件。
3. 半年报告之后存在重复总结与确认页面。

本批完成标准是：审核发现可重复验证，三个专项各有一个具体、简单、可实施的推荐方案，共享字段与存档／流程约定清楚，统筹能够据此审核并安排第一批开发。

不要交付只有方向口号的计划，也不要提前修改游戏行为。遇到产品取舍，先提出有依据的推荐答案，集中放入方案末尾供统筹审核。

## 2. 开始前

1. 阅读当前适用的 AGENTS.md 和主台账全文，重点看第 4、5、6、10、11 节。
2. 核对 `git status --short`、HEAD、当前分支和真实代码。规划基线为 `3f290407a299790665b58c4bc7c2bf8455d24bbd`；不同则说明差异，不强行回退。
3. 当前 README 修改、升级规划和本指令属于已存在的规划工作；`outputs/019facd8-3fee-7b80-bfcd-2d1d35e522ba/` 是更早的未跟踪目录。保护它们，不清理、不覆盖、不归入你的新增改动。
4. 开始实际执行后，将 S0-02 标为“进行中”，写入执行任务标识；不要预先把 S0-03 标为完成。

## 3. S0-02：固化实施基线

### 3.1 复现和证据

- 按主台账第 11 节重建老将训练、回国报价两个定向复现。使用相同状态、种子、位置、年龄和参数，记录实际俱乐部 ID。
- 将复现做成可重复执行的测试或脚本，复用现有测试框架，不引入新依赖。可以新增专用测试文件；禁止为让复现通过而修改生产公式。
- 历史结果“35 岁五种焦点相同”“100 个种子、300 份报价、国内 0”是待复核基线。若当前结果不同，如实记录并查明原因，不强改断言制造一致。
- 核对报告→结束页→下一半年这条链路，枚举各条件下真实调用的 store 动作、phase 和按钮。记录哪一步只是重复确认，哪些入口涉及必要决定。
- 在当前依赖可用时运行现有全量测试和上述定向复现。记录命令、退出码、测试数量与结果；已有历史通过记录不能替代本轮结果。无需为纯基线取证进行全套视觉改版或安装额外工具。

### 3.2 匿名兼容夹具

- 用确定种子和现有公开 store 操作生成匿名基线生涯，不读取或覆盖用户浏览器存档。
- 固化至少：HALF_YEAR_PLAN、SPECIAL_EVENT_RESULT、HALF_YEAR_REPORT、PRO_STAGE_COMPLETE、已生成报价的 TRANSFER_WINDOW、合同到期状态、临近退休状态。
- 保存为当前持久化层认可的格式；用现有加载校验确认合法，并记录 saveVersion、dataVersion、phase、windowIndex、seed 和生成命令。
- 可以使用一个确定性生成器产出多个场景，不手工堆造大量看似合法的 JSON。任何无法生成或加载的场景列为未完成，不绕过校验。

持久证据统一放在 `docs/evidence/CEU-20260907/S0/`，包含摘要、日志、夹具说明和可重复执行的方法；不要只保存在 `/tmp`。

## 4. S0-03：形成最小实施方案

新增 `docs/23-career-experience-implementation-proposal.md`，依次提交以下内容。

### T：老将训练

- 列出年轻、31–33 岁、34–36 岁、37 岁以后实际显示的选项；每个选项写清名称、适用条件、输入字段、具体效果、代价、上限和报告用语。
- 画出训练焦点、职业策略、低状态恢复、特殊事件、成长和衰退的执行顺序，明确重复增益如何避免。
- 以现有四项能力、三项状态和年龄曲线提出一套具体推荐参数。说明依据与需要开发后验证的部分；没有测量的参数不得标成已验证。
- 说明旧 trainingFocus 枚举如何继续解释，年轻阶段如何保持既有行为；不通过全面提高成长或取消衰退解决问题。

### M：转会方向

- 按“候选资格→报价组合→固定种子抽取”列出调整位置，常规市场、到期续约／自由转会和低出场救济分别说明。
- 明确当前求职方向的页面入口、修改动作、持久化字段，以及 draft 和 player 分别表达什么，避免改写开局档案。
- 首选约定：新偏好影响下一次尚未生成的市场；已生成报价及谈判结果保持不变。写清按钮、提示与连续修改／刷新时的行为。
- 明确三份外部报价的上限、续约是否另计、候选不足的回退顺序和测试判据；不新增球队搜索、无限报价刷新或大型市场子系统。

### F：半年流程

- 给出旧流程→新流程对照表，列明每类报告末尾主按钮、次要入口、前置条件、目标 phase 和是否推进时间。
- 覆盖普通窗口、有报价、合同到期、连续失约、继续留队、自愿／强制退役、退出国家队和复查报告。
- 解释重复点击、刷新和回看为什么不会重复模拟、发薪、记比赛或推进窗口。
- 优先保留旧 PRO_STAGE_COMPLETE 的兼容恢复路径；不为减少一个页面而重写全部状态机。

### 共享契约与首个开发批次

- 对 D-T1、D-T2、D-M1、D-M2、D-M3、D-F1、D-C1 分别给出推荐答案；全部标为“建议，待审核”。
- 列出实际需要改动的文件、字段、枚举和 store 接口，注明是否涉及存档迁移或版本变化。复用有歧义的旧字段不等于零兼容成本。
- 给出 T-01 至 T-04 的下一批执行范围与验收用例，说明哪部分适合 Astra 轻度直接实施，哪部分因迁移或状态复杂度建议升为中度。
- 只提交一套首选方案；重要替代方案最多列一项并说明未选原因，避免把大量未收敛选项交给用户。

## 5. 允许的改动与停止点

本批允许：新增基线测试／生成器、匿名夹具、证据和方案文档；更新主台账的相关任务、建议、风险、日志与检查点。

本批不修改：生产游戏逻辑、页面行为、数值参数、存档版本、运行依赖或发布配置。不删除既有测试，不清理无关文件，不提交、推送、部署或发布，不启动并行 AI 代理。

如果现有接口无法支持某个复现，不先改生产代码；记录最小技术缺口和可行替代方法，交统筹判断。

S0-02 证据齐备后可记录完成；S0-03 方案交付后标“待验收”。**停在统筹审核处，不标记 S0-04 完成，不开始 T-01 或 T-02。**

## 6. 交回统筹的结果格式

1. 当前 HEAD、本批改动文件与工作区保护情况。
2. S0-02、S0-03 的真实状态和交付物链接。
3. 两个规则复现、流程基线和兼容夹具的核验结果。
4. 七项 D-* 决策的推荐答案摘要。
5. 测试命令与结果；失败、未执行和局限单列。
6. 建议的首个训练开发批次、需要统筹或用户决定的事项。

结束前更新主台账第 10 节，让下一个参与者只读文档就能继续。完成以上交付后停止，等待统筹审核。
