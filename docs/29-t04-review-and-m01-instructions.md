# T-04统筹验收与市场专项M-01交接

日期：2026-09-07；统筹：Codex；HEAD：`3f290407a299790665b58c4bc7c2bf8455d24bbd`，生产改动未提交。

## 1. 结论及边界

**T-04通过，T-01至T-04全部完成；训练专项按已批准范围收口。下一步M-01待用户授权转交，未启动。**

本结论不等于全项目验收或发布批准。参数继续保持状态最多+4、竞技−2／身体−3、身体衰退×0.8；未发现需要修改规则的证据。真实手机、其他浏览器、完整生涯最终平衡与持久v11备份/发布回退仍分别留在Q／发布前门槛，不因训练专项完成而自动通过。

## 2. 本轮独立核验

| 项目 | 结果 |
| --- | --- |
| 产物与范围 | 105项T-04 artifact-hashes的SHA和字节数匹配；377份本批前文件中373份不变，仅docs/01、02、05、20改变。生产src、冻结证据和outputs未变 |
| 离线审计 | 执行audit.py通过；另从90条逐窗记录独立重算9分支状态范围、四能力净减少及负变化累计，与long-summary一致 |
| 旧档恢复 | 统筹在全新Chromium上下文独立复跑17份真实＋4份构造envelope，全部经合法后继新增一次报告并保存重载；旧前缀保护通过 |
| 跨年龄 | 独立复跑30→31、33→34、36→37三组；每组两报告，页面返回保留选择、新窗默认和报告/保存一致 |
| 长期观察 | 独立复跑3固定种子×3政策×10窗，90窗完成，issues/errors为空。除补充cashEuro诊断外，整个verification结果与原交付一致 |
| 工程 | 核对执行者定向49项、全量60文件415项、typecheck/build实际日志及代码哈希。本轮未重复运行未变化的工程套件；上轮统筹已独立通过同一生产代码的415项及构建 |

本轮采用已审阅脚本的临时副本，输出 `/tmp/ceu-t04-coordinator/`，访问自己启动的 `http://127.0.0.1:4188/`；不覆盖T-04冻结证据。隔离Chromium经工具审批运行，不访问用户浏览器profile。脚本变动限定本地端口、输出路径，以及verify通用日志的cash→cashEuro字段更正；比较时仅忽略这一新增诊断，旧原始日志保持原样。

E-01不阻塞验收：原通用现金诊断缺失已公开记录，不能作为逐步数字证据；原dedupe.json明确记录cashEuro，且有真实完整state相等断言。此次verify复跑补录了cashEuro，未发现输出或行为差异。今后新司机应直接使用cashEuro，不复制错误字段。

跨年龄起点保留原35岁历史，确实不构成自然时间线连续性证明；本批只用其验证年龄门槛与公开动作，不把它升级为Q完整生涯证据。9条长期路径到10窗即停止，未到退役终点；政策随事件/恢复分流，不作严格单变量对照或最终平衡宣称。

## 3. 页面与后续门槛

统筹另独立复跑pages、dedupe、event-flip、report-viewports四份脚本，四份结果JSON与原交付完全相等；17张新截图与原图逐字节一致。页面实际点击、旧DOM重复点击、45→49实际维护及46→45实际恢复均通过。原报告文本、收入、history保存重载不变，控制台error/pageerror为空；人工抽查320宽训练、430宽旧计划、375宽恢复与长报告、1366宽六项卡片。

复用T-03统筹已验收的1280×720与390×844；检查T-04新增1366×768、320×568、375×667、430×932截图，训练说明与旧饱和计划可读，正常滚动后按钮可点击。长摘要按保存字符串显示，完整元素截图与CTA视口截图分别解释。原其他统计区小字号、桌面留白不在本批重做范围。

后续Q-04仍须明确真实手机未验的处理；旧客户端读取v12及持久旧档恢复不能用内部滚动备份替代。大于500kB构建警告继续保留，不因本次通过隐藏或删除。

## 4. 下一批ASTRA-M01完整指令（待用户授权转交）

建议配置：GPT-6 Astra轻度。M-01只准备市场变更前基线与验收案例；市场规则及共享状态实现另分派M-02/M-03，不从本文件推导后续开发授权。

```text
你执行《上场》CEU-20260907的ASTRA-M01，配置GPT-6 Astra轻度，Codex主任务统筹。
工作区：/Users/zhihu/Documents/足球生涯游戏。
只有用户明确转交本指令授权M-01后启动。本批仅市场资格/组合/恢复的变更前基线、测试与证据准备，不修改生产行为，不进入M-02/M-03/F/Q，不提交推送部署，不创建并行代理。

先读适用AGENTS.md、docs/20第4/5/6.2/7/10节、docs/23第2/4节、docs/29全文、S0市场/流程证据、docs/21及T-04索引。核验HEAD、git status和本批前哈希。预期HEAD为3f290407a299790665b58c4bc7c2bf8455d24bbd，本地未提交、版本12/12；训练T-01至04已验收，保护全部已有生产改动、冻结数据与outputs，不访问用户真实浏览器存档。
收到用户授权后，将主台账顶部、M-01行、第10节统一更新为进行中，记录本次转交范围。

目标：为下一批修复国内机会资格、报价组合和后续不重抽提供可复现的前后对照，严格区分当前缺陷、当前正确行为、已批准目标和未知缺口。不要把原缺陷断言称为新功能通过。

1. 按资格→组合→固定种子抽取顺序，核查当前transfers.ts的常规成人、到期续约/自由转会、海外豪门低出场救济三条路径，附代码位置与事实。明确年龄、OVR、平台/队伍层级、当前球队排除、偏好、角色改善、市场开启条件分别属于哪一层。抽样没出现不能当作不在资格池的证明；私有资格函数用源码分析佐证，不为测试方便修改生产导出。
2. 原S0的E-03输入和100个固定种子只读复用，验证当前v12市场仍与原市场行为一致，保存新的复跑结果，不覆盖旧JSON。已有缺陷是高OVR的非DOMESTIC方向硬排除国内，不重解释成随机概率低。
3. 先写有限案例表，再运行。覆盖OVR79/80/84/85、17/18/33/37/40、国内/海外现队、STRONG/CONDITIONAL/DOMESTIC；按风险选代表组合与边界，不做所有条件×100种子的巨大全组合。三种方向保留相同能力/种子成对比较；其他边界少量固定种子即可。
4. 把已确认目标写为未来验收清单：CONDITIONAL在两地均有合格候选时各至少1份；DOMESTIC候选充足时至少2份国内；STRONG保留海外优先及世界级高平台目标，国内仍可进入开放槽；外部最多3份、到期续约另计总数最多4。救济必须角色改善；未满18、最后生涯窗口、候选不足、无报价、去重、排除现队保持合理限制。不自行改变文档23锁定规则。
5. 冻结必要的当前v12匿名存档和生成输入：常规报价、到期含续约、救济路径、已选择报价、谈判状态（成功/失败/撤回按实际可达性列覆盖）。优先公开store流程，从已有匿名合法档继续；短暂状态可在测试Storage写入时捕获。不手改phase把结果伪装成真实旧档；构造边界另目录标注源SHA和全部差异。存档用真实save/load校验，记录种子及可复演动作。
6. 同时记录后续M-03需要保护的数据：draft开局档案、player当前方向、历史、已有报价全部字段、selectedTransferChoiceId及谈判结果。重点核查到期优先重生成、空TRANSFER_WINDOW、回看报告后再次进入市场的当前行为。只证明当前现状和风险，不提前修复防重抽或F导航；若现有合法存档不能靠phase/window/history区分，给出具体样本交统筹，不能猜造市场ID。
7. 新样本搜索最多100个预先固定种子，每种子最多2条预定生涯策略，达到覆盖即停；只补已有证据缺口。难以达到的路径保留未覆盖、尝试范围及构造补充，不伪称自然可达，不无限增加模拟。
8. 新证据只写docs/evidence/CEU-20260907/M-01/，附manifest、SHA、来源类型、采集前生产基线、命令、输入输出、路径覆盖和复跑方法。生成器验证当前生产源码哈希/12版本/固定输出目录，独占写入，正常测试只读，不覆盖任何T/S0证据。
9. 测试冻结旧行为与未来目标明确分开。新增测试可以描述当前已知缺陷，但注明M-02实现时应替换为目标断言，保留旧JSON。不能为了让未来目标立即通过而修改市场生产代码。验证每份档独立加载、合法后继、保存重载，现金字段使用cashEuro，事件/窗口/history变化明确记录。
10. 运行本批相关定向测试及typecheck；无生产改动无需重复build或全套页面验收。如新测试干扰现有套件或发现失败，再做必要回归。未执行项写清，任何失败不得改快照掩盖。

交付：市场三路径事实/目标对照表、有限边界案例、匿名存档及生成输入、测试与完整证据索引、缺口和风险、M-02/M-03精确改动与验证交接。更新主台账与docs/21市场栏，M-01标待验收；M-02及后续仍未开始。T专项已通过的结论不重开，除非出现具体回归证据。
完成后停止，由用户转交Codex审核；不自行批准基线缺口，不开始生产修改。
```

## 5. 当前检查点

训练专项已完成本地技术验收，参数保留，尚未发布。下一步为用户授权转交ASTRA-M01；M/F/Q/R均未启动。本轮统筹只更新验收状态与文档，不改生产文件。
